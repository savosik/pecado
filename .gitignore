*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/.codex
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Playwright
test-results/
playwright-report/
playwright/.cache/

# WSL2 metadata
*.Zone.Identifier

# Compiled views
/storage/framework/views/

# Laravel Boost (auto-generated)
CLAUDE.md
AGENTS.md
boost.json
/.claude/skills/
/.claude/scheduled_tasks.lock
/reference/
/reference

# Server credentials (local only — содержат пароли)
docs/DEV_SERVER_CREDENTIALS.md
docs/PROD_SERVER_CREDENTIALS.md
docs/ERP_1C_PROD_CONNECTION.md
docs/ANDREY_ESB_CONNECTION.md

# Локальный compose override — каждый разработчик ведёт свой
docker-compose.override.yml

# Лёгкий локальный профиль supervisor-воркера (numprocs=1),
# генерируется `make worker-slim` из docker/supervisor/conf.d/
docker/supervisor/conf.d.local/

# AsyncAPI и MkDocs: трекаем сборку в git — деплоится через rsync

# Временные файлы и скрипты разработки
/tmp/

# Генерируется командой sitemap:generate (своё на каждом окружении)
/public/sitemap.xml

# Локальные SEO-материалы (семантика, ТЗ, канбан) — не версионируем
/seo/

# Материалы от Andrey Company (докуметация API поставщика, токен) — локально
/docs/andrey/

# Конкурентная разведка — локальные скрипты с dev-кредами, работают с tmp/insides
/scripts/insights/

# Runtime-темп медиабиблиотеки (spatie) — не версионируем
/storage/media-library/
.env.bak-*
