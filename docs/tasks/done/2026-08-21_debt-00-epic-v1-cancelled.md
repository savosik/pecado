# ЭПИК (v1): дебиторка — светофор долга, касания и самообслуживание оплаты

> **ОТМЕНЁН 27.08.2026.** Заменён эпиком [debt-00 v2](../backlog/2026-08-27_debt-00-epic.md)
> — лестница по времени и критичности вместо скоринга.
>
> Причина: независимый разбор на прод-данных показал, что автоматика затронула бы
> 14 клиентов (просрочка > 5 000 ₽), 78 % просрочки — три клиента, а гейт чекаута
> видит лишь 36 % денег по заказам (остальное менеджеры заводят в 1С). Одиннадцать
> карточек и восемь таблиц ради этого — тот же «боинг», только по сущностям.
> Заказчик: «главное не переусложнять».
>
> Карточка не удалена намеренно: в ней решения 21.08 (отсечка, grace, «норма
> невидима», отправители, Гевея), цифры с прода и разбор «отгрузка после оплаты».

**Приоритет:** высокий
**Создано:** 2026-08-21
**Карточки:** `debt-01` … `debt-11`
**Флаг:** `DEBT_CONTROL_ENABLED` (+ `config/debt.php` — создан в debt-01; боевое включение поштучно по ступеням через `DEBT_CONTROL_LIVE_ACTIONS`)
**Актуализировано:** 2026-08-27 — см. раздел «Что изменилось после постановки»

| Волна | Карточки | Что даёт |
|---|---|---|
| 0 — доверие к цифрам | `debt-01` | Сверка балансов с 1С, пилотное включение финансов кабинета |
| 1 — скоринг | `debt-02` | Машина уровней (светофор), shadow-режим, журнал «почему» |
| 2 — касания и платёжки | `debt-03`, `debt-04` | Письма/баннеры эскалации — свои типы уведомлений в матрице; платёжка «бери и плати» |
| 3 — кабинет и ограничения | `debt-05`, `debt-06`, `debt-07` | Статус в кабинете, гейт в чекауте, мгновенная разморозка оплатой |
| 4 — гибкость | `debt-08`, `debt-09` | Обещания оплаты, диспуты; контур менеджера и ручная пауза |
| 5 — досудебка и метрики | `debt-10`, `debt-11` | Претензионный пакет одной кнопкой; DSO и конверсия ступеней |

## Суть в одном абзаце

На проде (21.08.2026) долг клиентов 11,34 млн ₽, из них просрочено 2,60 млн на
54 контрагентах; 59 % просрочки — один клиент. Сегодня взыскание держится на памяти
двух менеджеров. Эпик строит **машину уровней**: каждый клиент находится в одном из
четырёх состояний — `чисто → напоминания → только предоплата → стоп` — и переходы
между ними порождают действия автоматически: касания — типы уведомлений матрицы (note-00) через `MailStream`,
ограничения через гейт чекаута, задачи менеджеру через CrmTask. Вниз (ужесточение)
система движется медленно и планово, вверх (смягчение) — мгновенно по событию оплаты
из 1С, без участия людей. Клиент гасит долг самообслуживанием: готовая платёжка
(PDF + файл для клиент-банка + QR), сценарии «погасить просрочку / весь долг /
предоплата», отправка бухгалтеру в два клика.

## Анти-боинг: принципы управляемости (зафиксировано обсуждением)

Риск «панели боинга» — главный проектный риск. Защита:

1. **Ручки — в конфиг, не в UI.** Пороги/grace/гистерезис живут в `config/debt.php`
   и меняются релизом. РОП в интерфейсе работает со списком клиентов, а не с настройками.
2. **Одна ручка на клиента.** Никаких индивидуальных порогов. Три профиля
   (`regular` / `marketplace` / `legal`) + единственный оверрайд «пауза до даты
   с причиной» (менеджер ≤ 7 дней, РОП ≤ 30; бессрочной нет ни у кого).
3. **Четыре состояния, не больше.** Всё, что хочется сделать пятым уровнем, —
   это действие внутри существующего (блок предзаказов = действие позднего жёлтого).
4. **Конституция в коде** (невыключаемые инварианты):
   - автоматика никогда не ограничивает без предшествующего письма-предупреждения;
   - никакой эскалации по данным старше `debt.stale_after_days` (`balance_erp_updated_at`);
   - любое ограничение снимается оплатой автоматически;
   - профили `marketplace`/`legal` в автоматику не входят;
   - у каждого перехода и касания есть однострочный ответ «почему» (explain).
5. **Shadow-режим до боевого**: минимум 2 недели система пишет журнал «я бы сделала X»,
   ничего не делая; включение — поштучно по ступеням.

## Решения, принятые при постановке (21.08.2026)

| Вопрос | Решение |
|---|---|
| Верхняя ступень | «Финансовый арест»: вход и раздел оплат открыты, покупки закрыты. `UserStatus::BLOCKED` (логаут-блок) остаётся ручным инструментом админа вне лестницы |
| Кредитный лимит | Мастер — `agreements` из 1С; фолбэк — 50 % среднемесячного оборота за 90 дней; ручная правка РОП поверх. **Клиенту лимит не показывается** — знание лимита стимулирует выбирать его до дна |
| Отсечка значимости | Просрочка < 5 000 ₽ не влияет на уровень («проблема копейки»: 18 из 53 просрочек < 1000 ₽) |
| Grace | 5 банковских дней после срока платежа |
| Подача в кабинете | «Норма невидима»: у чистого клиента никакой шкалы нет; статус появляется только при значимой просрочке. Следующую ступень и даты переходов не показываем |
| Отправитель писем | Мягкие — от персонального менеджера; предупреждения об ограничениях и арест — от компании |
| Предзаказы | Закрываются на позднем жёлтом (`reminder` дольше N дней) — раньше обычных заказов |
| «Только предоплата» | Чекаут работает: заказ создаётся на сайте в статусе «Ожидает предоплаты» и **не публикуется в 1С до оплаты**; сразу платёжка на сумму заказа; после оплаты уходит штатным `order.created`. Протокол 1С не меняется. (Уточнено 27.08: в контракте нет условий оплаты, пометка в комментарии — просьба, а не ограничение) |
| Авторазморозка | Просрочка упала ниже отсечки → ограничения сняты мгновенно по событию из 1С |
| Обещания | Фиксируют менеджер и клиент; у клиента — одно активное, ≤ 14 дней, после нарушения — только через менеджера |
| Диспут | Спорная сумма сразу исключается из скоринга; менеджеру задача с SLA 2 рабочих дня |
| Претензия | Авторасчёт процентов по ст. 395 ГК (менеджер может править); отправка всегда ручная |

## Данные, на которых стоит дизайн (prod, срез 2026-08-21)

| Факт | Число | Следствие |
|---|---|---|
| Долг / просрочка | 11,34 / 2,60 млн ₽ (86 / 54 контрагента) | Масштаб оправдывает автоматику |
| Просрочки < 1000 ₽ | 18 из 53 контрагентов | Абсолютная отсечка обязательна |
| Гевея: просрочка к обороту | 1,54 млн = 157 % месячного оборота | Критерий «доля от оборота» разделяет лучше абсолютных сумм |
| Железняк: то же | 85 тыс = 9 % оборота | Крупный долг ≠ проблемный клиент |
| «Полные» неплательщики | 5 клиентов, оборот за 90 дней = 0 | Профиль `legal`: лестница не для них, сразу претензия |
| Хроничность | 15 клиентов с 6+ просроченными строками | Сигнал «хроник» в скоринге |
| Балансы маркетплейсов | WB/ЯМ не обновлялись 17 дней | Гейт свежести данных обязателен |

## Что уже построено (не изобретать заново)

- **Почта (note-00, в бою с 27.08.2026)**: домен сообщает «случилось X» —
  `MailStream::capture(new Occasion('finance.…', clientUserId, companyId, subject, data))`;
  кому слать — решает `NotificationRouter` по матрице «партнёр × уведомление»
  (`notification_preferences` + умолчания в `config/mail_occasions.php`), отправляет
  `NotificationDispatcher`/`SendNotificationJob` с окном склейки. Финансовые уведомления
  `finance.payment_due_soon` / `overdue_started` / `overdue_grew` / `overdue_cleared`
  уже есть, их собирает `Mail\Sources\FinanceScanner` (`mail:finance-scan`).
  Просрочка — состояние: ступень входит в `origin_key`, повтор письма не создаёт.
  Адресаты типизированы (`login` / `manager` / `contact_role` / `contact` / `email`),
  роль `accountant` из `client_contacts` сужается до контрагента письма. Стоп-лист
  `notification_suppressions`, вложения к письму — по образцу `MailStream::attachInvoice()`.
- **Математика (fin-00, регистр в бою с 14.08, старое ядро снесено 25.08.2026)**:
  единственный источник денег — лента регистра взаиморасчётов `settlement_entries`
  (`amount − settled_amount`, `amount_rub` фиксирован учётом, `document_kind`,
  срок в `date`, разрезы контрагент/наше юрлицо/валюта в строке). Читатель —
  интерфейс `PaymentForecast` в единственной реализации `LedgerPaymentForecastService`
  (`aging()`, `overdueOnly()`, `topDebtors()`, `debtTotal()`, `balances()`);
  для кабинета — `CabinetSettlementFinance` (fin-10). `contractor_balances` остаётся
  проекцией баланса 1С (`balance.updated`) — по ней гейт свежести и сверка.
  **Заказ — план, а не долг**: строки `document_kind = 'order'` в просрочку не входят
  (круг 12) — та же граница обязательна в скоринге.
- **Реквизиты**: `organizations` (наши юрлица: ИНН/КПП/БИК/счета, отсекать `is_stub`),
  `company_bank_accounts` (счета плательщика).
- **Акты сверки**: `ReconciliationService` + кабинет + XLSX + печатная форма
  `RECONCILIATION_ACT` из 1С — готово целиком.
- **Блок аккаунта**: `UserStatus::BLOCKED` + `EnsureUserIsNotBlocked` (остаётся вне лестницы).
- **Задачи**: `CrmTaskService::create()`, образец идемпотентности — `CrmTaskRecurrenceService`.
- **Точка врезки гейта**: `CheckoutService::checkout()` — единственный вход всех путей заказа.

## Границы скоупа

| Выпилено | Причина |
|---|---|
| ~~Изменение протокола 1С~~ | Домен целиком сайтовый: потребляет `balance.updated`/`payment.created`, публикует обычный `order.created` |
| ~~Онлайн-эквайринг~~ | B2B платит платёжками; «оплата» = поступление в 1С |
| ~~Индивидуальные пороги per-клиент~~ | Анти-боинг: только профили + пауза |
| ~~Показ кредитного лимита клиенту~~ | Стимулирует сидеть под завязку лимита |
| ~~Логаут-блокировка в лестнице~~ | Отрезает канал самопогашения |
| ~~Автоотправка претензий~~ | Досудебка — всегда решение человека |
| ~~Пятый уровень светофора~~ | 4 состояния — предел объяснимости |

## Порядок и зависимости

`debt-01` → `debt-02` → (`debt-03` ∥ `debt-04`) → (`debt-05` ∥ `debt-06`) → `debt-07` →
(`debt-08` ∥ `debt-09`) → (`debt-10` ∥ `debt-11`)

Инвариант «блок только после письма» делает `debt-03` блокером `debt-06`.
`debt-07` (разморозка) обязана выйти в один релиз с `debt-06` (ограничениями) —
ограничивать без мгновенной авторазморозки запрещено.

## Что изменилось после постановки (актуализация 27.08.2026)

Постановка 21.08 опиралась на две подсистемы, которых больше нет:

| Было в постановке | Стало | Следствие для эпика |
|---|---|---|
| Пульт уведомлений (событие → условия → получатели), конструктор правил, `DeliveryGuard`, `notification_deliveries`, `live_events` | Снесён 22.08. Поток писем с правилами-фильтрами (mail-00) снесён 27.08. Действует **матрица уведомлений** note-00: `MailStream::capture(Occasion)` → `NotificationRouter` → `NotificationDispatcher`; настройки — строка «партнёр × уведомление» | Никаких правил/условий/расписаний в UI. Касание = **тип уведомления** в `config/mail_occasions.php` + `capture()` в доменном коде. Каскад и ритм живут в коде (сканер), как у `documents.reconciliation_weekly` |
| Долг из `contractor_balances` + графики `shipment_payment_schedules` + `payment_allocations` + `contractor_balance_overdue_details` | Три таблицы сброшены 25.08 (fin-11). Всё считается **лентой регистра `settlement_entries`** через `LedgerPaymentForecastService`; `shipments.paid_amount`/`orders.prepaid_amount` пишет только `SettlementProjector` | Сигналы скоринга, расшифровки в кабинете, состав платёжки и претензии — из `settlement_entries`. Расчёт сайта и 1С теперь один источник — сверка debt-01 сводится к `contractor_balances` (баланс) против регистра (лента) |
| Клиентские письма шли зашитыми листенерами | **Все клиентские уведомления выключены умолчанием** (решение 27.08); включённое — только `documents.reconciliation_when_debt` | Решение 27.08: эскалация — **свои типы уведомлений `finance.debt_*`** со строками в матрице, включённые умолчанием (редкие: только переходы уровня и шаги каскада); существующие `finance.*` клиентам не включать — их масса утопит эскалацию |

Карточки `debt-01…05`, `07`, `10`, `11` актуализированы под это. Принципы анти-боинг и
решения 21.08 не менялись. Дополнительно: `config/debt.php` и `debt:verify-balances`
уже существуют (debt-01), пилотный список кабинета — `CABINET_FINANCE_PILOT_USERS`.
