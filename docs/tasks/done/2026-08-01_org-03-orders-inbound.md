# org-03 · Организация и склад в заказах (приём от 1С)

**Приоритет:** высокий
**Создано:** 2026-08-01
**Закрыта:** 2026-08-11 — разработка завершена, приёмка не проводилась по решению заказчика
**Эпик:** [org-00](../backlog/2026-08-01_org-00-epic.md)
**Зависимости:** `org-01`, `org-02`

## Описание

Сайт начинает **сохранять и показывать** организацию и склад, на которые 1С провела
заказ. Организацию сайт **не выбирает и не отправляет** — это решение 1С. Заказы без организации
остаются валидными.

## Что делаем

**Миграция:** `orders` + `organization_id` (nullable, FK на `organizations`),
+ `warehouse_id` (nullable, FK на `warehouses`). Комментарии обязательны:
`->comment('Наша организация, на которую заказ проведён в 1С (organizations.id)')`,
`->comment('Склад отгрузки, определённый 1С (warehouses.id)')`.

**Handler-ы** `HandleOrderCreated`, `HandleOrderUpdated`:
- `organization.uuid` → `OrganizationResolver::resolveByUuid()` с hint-ом
  `{name, tax_id}` из payload;
- `warehouse_uuid` → `Warehouse::where('external_id', …)`; не найден — `null` + `Log::info`
  (не роняем обработку, как это уже сделано для товара и склада в `HandleStockUpdated`);
- поля **не сбрасываются** при отсутствии в payload — тот же принцип, что для
  `delivery_method` и `erp_created_at` (v15.3, v13.7).

**Смена организации в `order.updated`** — пишем запись в `OrderChangeLog`, чтобы
менеджер видел, что документ переехал между юрлицами.

**UI:**
- админка, карточка заказа — строка «Организация» и «Склад отгрузки»;
- админка, список заказов — фильтр по организации (включая «не указана»);
- ЛК клиента, карточка заказа — «Продавец: {организация}» (гейт `ORGANIZATIONS_ENABLED`);
- CRM, карточка заказа — то же, что в админке.

Всё под флагом `ORGANIZATIONS_ENABLED` в части отображения; сохранение — всегда.

## Edge-кейсы

- **Организация не пришла** (legacy или 1С ещё не научилась) → `null`, в UI строка
  не показывается вовсе (не «—», это шумит в списке заказов).
- **Организация пришла впервые для существующего заказа** — нормальный сценарий
  переходного периода, просто проставляется.
- **Организация сменилась** при `order.updated` → перезаписываем + `OrderChangeLog`.
- **Склад пришёл, организация нет** (и наоборот) — оба поля независимы, частичное
  заполнение допустимо.
- **Склад — некондиция или «Москва подарки»** → сохраняем как есть, спец-обработки нет.
- **1С прислала склад, которого нет на сайте** → `warehouse_id = null` + лог. Заводить
  склад автоматически **не** нужно: справочник складов ведётся отдельно и влияет на
  витрину через регионы.
- **Заказ создан сайтом, 1С прислала `order.created` с тем же UUID** (upsert) —
  организация и склад из payload **перезаписывают** предложенные сайтом значения
  (принцип 6 эпика), расхождение пишем в лог как метрику.

## Замер покрытия на проде (2026-08-11)

| Документ | Покрытие организацией |
|---|---|
| Реализации | **100 %** (7 453 из 7 453) |
| Заказы | **95,7 %** (5 944 из 6 214) |

**Бэкфилл 270 непокрытых заказов рассматривался и отклонён.** Выводить организацию
не из чего: у 269 из 270 нет ни одной позиции реализации, а 266 стоят в статусе
`ready_for_shipment` — они ещё в работе.

Единственный оставшийся источник — вывод по другим документам клиента, и это была бы
догадка. Организация у документа реквизит юридический; записав её предположением
на заказ, который 1С ещё отгрузит под своим юрлицом, мы получили бы расхождение,
которого никто не заметит. Пустое поле честнее неверного.

Гипотеза «организация неизвестна до отгрузки» проверена и **не подтвердилась**:
34 заказа в том же статусе `ready_for_shipment` организацию имеют, как и 126 заказов
без единой отгрузки. Закономерности в непокрытых 270 не видно, диапазон дат
15.04–20.07.2026, после 1 августа таких заказов нет вовсе.

→ **Вопрос к 1С:** почему по этим заказам организация не пришла. Тем же письмом,
что и про две организации-заглушки.

## Критерии готовности

- [x] Миграция с комментариями, `db:comments:audit` чист
- [x] Организация и склад видны в админке и ЛК
- [x] Фильтр по организации в списке заказов админки, включая «не указана»
- [x] Интеграционный тест: `order.created` с организацией → заказ привязан
- [x] Интеграционный тест: `order.created` **без** организации → заказ создан, поля `null`
- [x] Интеграционный тест: `order.updated` без поля организации **не сбрасывает** её
- [x] Интеграционный тест: неизвестный `organization.uuid` → создана stub-организация
- [x] Интеграционный тест: смена организации пишет запись в `OrderChangeLog`
- [x] `bi:sync-grants` применён на проде 2026-08-01; шаг автоматизирован в деплое
- [x] CRM, карточка заказа — сделано 2026-08-04, после появления карточек документов
      внутри CRM (коммит «заказы и реализации открываются внутри CRM»)
- [x] Колонка «Организация» в списке заказов админки и склад рядом с ней
- [x] Фильтр по складу отгрузки в списке заказов админки, включая «не указан»
- [~] Визуальная проверка — вынесена в [чек-лист приёмки эпика](../backlog/2026-08-01_org-00-epic.md#чек-лист-приёмки)

## Что сделано (2026-08-01)

| Файл | Назначение |
|---|---|
| [миграция](../../../database/migrations/2026_08_01_140000_add_organization_to_orders_table.php) | `orders` + `organization_id`, `warehouse_id` (nullable FK, `nullOnDelete`) |
| [ResolvesDocumentOrganization](../../../app/Services/Erp/Support/ResolvesDocumentOrganization.php) | общий разбор `organization` + `warehouse_uuid`, переиспользуется в `org-04` |
| [HandleOrderCreated](../../../app/Services/Erp/Handlers/HandleOrderCreated.php), [HandleOrderUpdated](../../../app/Services/Erp/Handlers/HandleOrderUpdated.php) | приём полей |
| [OrderChangeLogger](../../../app/Services/Order/OrderChangeLogger.php) | «Организация» в журнале изменений заказа |
| [Admin\OrderController](../../../app/Http/Controllers/Admin/OrderController.php) | карточка + фильтр списка (`none` = «не указана») |
| [User\OrderController](../../../app/Http/Controllers/User/OrderController.php) | `seller` в карточке ЛК под флагом |
| тесты | `OrderOrganizationTest` (11), `OrderOrganizationUiTest` (6) |

### Решения, принятые по ходу

**Явный `null` в `organization` не сбрасывает сохранённое значение.** Карточка требовала
только «отсутствие поля не сбрасывает». Расширил и на явный `null`: у проведённого
документа организация есть всегда, поэтому `null` от 1С означает «не знаю», а не «очисти».
Потерять связь необратимо, а восстановить — только повторной выгрузкой.

**Неизвестный склад пишется как `null`, а прежнее значение затирается.** Так и было
в карточке, но стоит зафиксировать причину: если 1С говорит «проведено на склад X»,
а X сайту неизвестен, показывать старый склад Y — прямая дезинформация. Пустое поле
честнее.

**Заглушка организации клиенту не показывается.** У заглушки вместо названия лежит UUID.
В админке она видна с бейджем, в ЛК блок «Продавец» просто не выводится.

⚠️ При этом `is_stub` обязателен в выборке связи (`organization:id,name,...,is_stub`) —
без него проверка молча не срабатывает, и клиент видит UUID. Тест на это есть.

## Витрина достроена (2026-08-04)

| Файл | Что добавилось |
|---|---|
| [Admin\OrderController](../../../app/Http/Controllers/Admin/OrderController.php) | склад в выборке и в строке списка, фильтр `warehouse_id` (`none` = «не указан») |
| [Admin/Pages/Orders/Index.jsx](../../../resources/js/Admin/Pages/Orders/Index.jsx) | колонка «Организация» со складом второй строкой, фильтр «Склад отгрузки» |
| [Crm\DocumentController](../../../app/Http/Controllers/Crm/DocumentController.php) | организация с флагом `is_stub`, гейт `ORGANIZATIONS_ENABLED` |
| [ClientTimelineService](../../../app/Services/Crm/ClientTimelineService.php) | организация и склад в записях ленты, фильтр документов по организации |
| тесты | `ShipmentOrganizationUiTest` (5), `CrmDocumentOrganizationTest` (10), +3 в `OrderOrganizationUiTest` |

Решения:

**Организация и склад — одна колонка, а не две.** Менеджер читает их вместе («чьё юрлицо
и откуда уехало»), а два столбца съели бы полэкрана в и без того широком списке заказов.
Склад идёт второй строкой подписью «Склад: …», чтобы не путался с организацией.

**Фильтр по организации в CRM выкидывает записи менеджеров из ленты.** У комментария,
задачи, письма и звонка юрлица нет — оставить их в выдаче значило бы утверждать, что они
относятся к выбранной организации.
