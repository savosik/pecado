# fin-04 · Приём движений: хендлеры, очередь, проектор

**Приоритет:** высокий
**Создано:** 2026-08-09
**Эпик:** [fin-00](2026-08-09_fin-00-epic.md)
**Зависимости:** `fin-02`, `fin-03`
**Волна:** 1 — аддитивно, старый путь продолжает работать

## Описание

Сайт начинает принимать и сохранять движения регистра. Читает их пока **никто** — переключение
чтения это волна 3. Смысл: данные должны начать копиться до того, как на них что-то завяжется,
иначе первое же переключение упрётся в пустые таблицы.

## Что делаем

### Хендлеры (`app/Services/Erp/Handlers/`)

| Хендлер | Логика |
|---|---|
| `HandleAgreementCreated` / `Updated` / `Deleted` | `updateOrCreate` по `uuid`; резолв сторон существующим трейтом [`ResolvesContractorParty`](../../../app/Services/Erp/Support/ResolvesContractorParty.php) и [`ResolvesDocumentOrganization`](../../../app/Services/Erp/Support/ResolvesDocumentOrganization.php). Удаление — мягкое |
| `HandleSettlementOpeningBalance` | Одна строка `type=opening_balance`, `date=as_of_date`. Повторная доставка **заменяет**, а не добавляет |
| `HandleSettlementPosted` | **Полная замена** движений документа: удалить все `settlement_entries` с этим `document_uuid` и `nature=fact`, создать заново из `entries[]`. Затем `SettlementProjector` |
| `HandleSettlementReverted` | Удалить движения документа. Строки графика по нему — тоже |
| `HandlePaymentScheduleUpdated` | Полная замена строк `nature=plan` в скоупе `document_uuid` |

Общий трейт `app/Services/Erp/Support/ResolvesAgreement.php` — резолв соглашения по `agreement_uuid`
с ленивым backfill, по образцу `ResolvesContractorParty`.

**Delete-and-recreate, а не diff** — тот же приём, что в `PaymentAllocationService::sync()` и
`HandleShipmentCreated::syncItems()`. Отличить «строка не изменилась» от «строки больше нет»
по дельте нельзя.

### Конвертация валюты

Рублёвый эквивалент **приходит из 1С** полем `amount_rub` (`СуммаРегл`) — сайт его не считает.
Для рублёвых движений `amount_rub = amount`. Если поле не пришло для валютного движения —
`NULL` + warning; подставлять курс из своего справочника нельзя, акт сверки разъедется с 1С.

### Регистрация

- `ErpMessageValidator::SCHEMA_MAP` (`app/Services/Erp/ErpMessageValidator.php:26-75`) —
  восемь новых событий;
- `ErpRevisionGuard::DOCUMENTS` (`app/Services/Erp/ErpRevisionGuard.php:49-61`) —
  `settlement.*`, `agreement.*`, `payment_schedule.*`;
- `SetupRabbitMQTopology` (`app/Console/Commands/SetupRabbitMQTopology.php:63-77`) — очередь
  `erp_in.settlements` ← `settlement.*`, `payment_schedule.*`; `agreement.*` добавляется в
  binding существующей `erp_in.contractors`;
- `docker/supervisor/conf.d/worker.conf` — воркер `erp_in.settlements`, `numprocs=2` (224 632 движения за год — объём умеренный).

### `SettlementProjector` (`app/Services/Settlements/SettlementProjector.php`)

**Единственный писатель** денормализованных проекций. Пересчитывает:
- `shipments.paid_amount / payment_status / paid_at` — теперь «закрыто по данным 1С»;
- `orders.prepaid_amount`;
- `settlement_checkpoints` (из `settlement.checkpoint`, не из движений).

Правила, унаследованные от `PaymentAllocationService` (они себя оправдали):
- пересчёт — **полная функция от состояния БД**, никогда инкремент;
- чанки по 500;
- `saveQuietly` + `withoutEvents` — иначе десятки тысяч лишних переиндексаций Scout;
- идемпотентность обязательна и покрывается тестом.

⚠️ Пока `SETTLEMENTS_LEDGER_ENABLED` выключен, проектор **не трогает** `shipments.paid_amount`:
эту колонку ещё пишет `PaymentAllocationService`, два писателя подерутся. До волны 3 проектор
ведёт только `settlement_checkpoints` и собственные агрегаты.

## Edge-кейсы

- **Движение раньше документа** → `document_id = NULL`, читается по `document_number`.
  Доклейка при получении документа — в `HandleShipmentCreated`/`HandleOrderCreated` по
  `document_uuid`. Отдельных трейтов-доклейщиков **не плодим**: одна точка на все типы.
- **Повторная доставка того же `settlement.posted`** → результат идентичен. Тест обязателен.
- **Устаревшая ревизия обогнала свежую** → `ErpRevisionGuard` отбрасывает, статус `'stale'`
  в журнале шины.
- **`entries[]` пуст** → все движения документа удаляются. Это валидный способ обнулить
  документ, не отменяя проведение.
- **Неизвестный `type`** → сообщение в DLQ целиком. Пропустить строку молча нельзя: баланс
  разъедется, и никто не заметит.
- **Контрагент или соглашение не найдены** → движение сохраняется с `NULL` в связи + warning.
  Деньги важнее связей; доклеится позже.
- **Гонка очередей**: `erp_in.settlements` и `erp_in.documents` независимы, порядок не
  гарантирован — та же ситуация, что уже описана в `rules/message-ordering.md`.

## Критерии готовности

- [x] Восемь хендлеров написаны, зарегистрированы, покрыты unit-тестами
- [x] Очередь `erp_in.settlements` создаётся `SetupRabbitMQTopology`, DLQ на месте
- [x] Воркер поднимается: `docker exec pecado-worker supervisorctl status`
- [x] `SettlementProjector` идемпотентен — тест повторного прогона
- [x] Интеграционные тесты через `ErpIncomingJob` (мок `AMQPMessage` + `fire()`), как в
      [`PaymentScheduleIntegrationTest`](../../../tests/Feature/Erp/PaymentScheduleIntegrationTest.php):
      - [x] повторная доставка не задваивает движения
      - [x] `settlement.posted` заменяет набор целиком
      - [x] `settlement.reverted` гасит вклад документа
      - [x] движение раньше документа доклеивается при его получении
      - [x] неизвестный `type` уходит в DLQ
      - [x] устаревшая ревизия отбрасывается
      - [x] валютное движение сохраняет `amount_rub` из 1С, свой курс не подставляется
      - [x] топология `erp_in.settlements` + DLQ
- [x] Ни одно существующее денежное чтение не изменило поведения — регрессия
      `Erp|Payment|Shipment|Order|Balance` 1330 тестов зелёная
- [x] `lint` чистый; `analyse` по новым файлам чистый (полный прогон PHPStan падает
      с exit 137 — earlyoom на локальной машине, известное ограничение)

## Что сделано (2026-08-11)

**Девять хендлеров, а не восемь.** `settlement.checkpoint` в списке карточки не было —
контрольная точка появилась в круге 3 переписки, когда 1С отказалась ручаться за сальдо
на 01.01.2026.

**Понадобилась четвёртая таблица — `settlement_documents`.** В карточке её нет, и это
не украшательство: `ErpRevisionGuard` сравнивает ревизию с уже применённой, а у движений
документа на сайте может не быть вовсе. Хранить отметку в самих движениях нельзя —
`settlement.reverted` их удаляет, и следующее устаревшее `settlement.posted` воскресило бы
отменённый документ, потому что сравнивать стало бы не с чем. Отсюда же `is_reverted`,
переживающий удаление строк. Покрыто тестом
«устаревшее проведение не воскрешает отменённый документ».

**Доклейка сделана наблюдателем, а не правкой шести обработчиков.**
`SettlementLinkObserver` висит на `Shipment`, `Order`, `Payment` и срабатывает только
при создании. Точек входа у документа больше, чем обработчик события: есть ещё
восстановление потерянного документа и ручное заведение в админке. Правя обработчики,
одну из них рано или поздно пропустили бы.

**Расхождение схемы и правил, найденное тестом.** `rules/payment-schedule.md` обещал
пропускать строку без `due_date` и сохранять остальные, а JSON Schema требует поле
и отбраковывает сообщение целиком. Права оказалась схема: частичный график показал бы
календарь, не сходящийся с суммой документа, и просрочку по неполному плану — молча.
Правило и критерий приёмки переписаны под это.

**Второе расхождение — `settlement_object_kind`.** Схема обещала подменять незнакомый вид
на `other`. Подмена стирает термин 1С без всякой выгоды: колонка строковая, а показывается
всё равно `settlement_object_name`. Схема, AsyncAPI и правила переписаны на «сохраняется
как есть», спека пересобрана.

**Найден и починен баг общего счётчика ревизий.** `settlement.posted` и
`payment_schedule.updated` — два независимых потока сообщений об одном документе,
но `revision` у них общая, документа. С единой колонкой `applied_revision` график
с той же ревизией, что уже применённое проведение, отбрасывался бы как устаревший:
`ErpRevisionGuard` считает устаревшим и равную ревизию. Клиент молча остался бы без
календаря платежей. Заведена отдельная колонка `applied_schedule_revision`, гвард
выбирает её по событию. Оба направления покрыты тестами: «график не отбрасывается
из-за ревизии проведения» и «устаревший график отбрасывается».

**`numprocs=1` у воркера, а не 2.** `settlement.posted` — полная замена движений документа
(delete + insert). Два воркера с двумя ревизиями одного документа дадут либо дубли, либо
применённую задом наперёд ревизию, и `ErpRevisionGuard` от этого не спасает: он сравнивает
с БД до вставки. 224 632 движения за год один воркер переваривает.

**Проектор пока делает только доклейку.** `shipments.paid_amount` и `orders.prepaid_amount`
не трогаются: эти колонки пишет `PaymentAllocationService`, и два писателя подерутся —
результат зависел бы от того, чьё сообщение доехало последним. Переключение — волна 3.

**Тесты:** `SettlementLedgerIntegrationTest` (17), `PaymentScheduleV16IntegrationTest` (9),
`SettlementTopologyIntegrationTest` (4, требует брокер), `SettlementProjectorTest` (4),
`HandleAgreementTest` (6). Все зелёные.
