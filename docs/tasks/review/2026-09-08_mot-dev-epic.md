# ЭПИК: блок CRM «Мотивация 2.0» — разработка (mot-20…mot-42)

**Приоритет:** высокий
**Создано:** 2026-09-08
**Ветка:** `feature/mot-motivation-v2`
**Основание:** ТЗ `docs/sales/tz/` (13 документов) и карточка `mot-00` — эпик написания ТЗ.
Все шестнадцать вопросов реестра закрыты 08.09.2026, разработка не блокирована.

**Вне эпика:** ERP-протокол (расчёт читает уже принятые из 1С данные и в шину не публикует);
согласование скидок и ценовые категории партнёра — данных нет ни в каком виде; отмена планов
на партнёра на `/crm/plans` — они остаются рабочим инструментом, из оплаты лишь исключаются.

## Что строим

Раздел `/crm/motivation` — рабочее место менеджера (11 экранов) и руководителя (12 экранов),
обслуживающее систему оплаты труда отдела продаж редакции 2.2. Формула:

```
Переменная = min(потолок, max(0, П1 + П2 + П3 − К1))
```

П1 — 1,9 % от базы сверх порога 60 % плана, П2 — 3 % по Новым партнёрам, П3 — 1 % по
Фокус-перечню, К1 — 0,05 % за календарный день от остатка просроченной задолженности.

## Архитектура: три места в ядре `payroll`

1. **`VariablePartComponent`** — компонент-контейнер по образцу `KpiBonusComponent`: владеет
   П1/П2/П3/К1, знает порядок, потолок и неотрицательность **на сумме** (п. 6.6 применяет их
   к П1+П2+П3−К1, а не к слагаемому). `PayrollCalculator` не трогаем — он по-прежнему суммирует
   только `ComponentKind::AMOUNT`. Расширить `KpiBonusComponent` нельзя: там зашита семантика
   «база × выполнение».
2. **Квартал — рядом, а не внутри.** `payroll_calculations` уникален по `(менеджер, месяц, версия)`;
   квартальная премия — на отдел, за квартал, с поклиентным зачётом. Отдельные таблицы, из месячного
   снимка только справочная ссылка (пп. 3.4 и 7.6 требуют невлияния на месяц).
3. **Схема v2 — новая строка в `payroll_schemes`** с `effective_from` = месяц запуска.
   `config/payroll.php['default_scheme']` не трогаем: на него завязан `PayrollCalculatorGoldenTest`
   (125 075,20), он обязан оставаться зелёным. `KpiBonusComponent` и `ActiveClientsMultiplier`
   остаются в каталоге и выключены в v2.

**К1 — это не `DisciplinePenaltyFactor`.** Тот считает задержку закрывающего платежа в рабочих
днях; К1 — интеграл остатка долга по календарным дням месяца. Источник — `debt_states` /
`DebtOverview::rows()` / `contractor_balances.overdue_debt`; мост `payroll_invoice_settlements`
нужен только ради срока оплаты по документу и льготного периода. Считать К1 по остатку на конец
периода нельзя: это наказывает за долги, которые работник как раз собрал (грубая оценка давала
44 640 ₽ вместо честных 27 098 ₽).

**Второго движка выручки нет.** Только `ShipmentAnalyticsService`; план и темп —
`PlanProgressService` / `ClientPlanFactService`; сигналы по партнёрам —
`OpportunityService::signals()`; рабочие дни — `Payroll\Support\WorkingCalendar` и
`config/production_calendar.php`; отсутствия — `manager_absences`.

## Решения, влияющие на код

| Решение | Следствие для разработки |
|---|---|
| Потолка К1 нет | ограничителя в компоненте не требуется; сверху вычет ничем не ограничен, кроме неотрицательности переменной части |
| Прогрессивной ставки П1 нет | П1 исчисляется от суммы базы за период; поклиентная разбивка — только объяснение на экране |
| Отдельная должность = персональные отклонения | у оклада, надбавок и всех четырёх ставок обязателен слой работника, а не только слой схемы |
| Автоматической заморозки расчёта нет | 5-го числа задача руководителю, месяц остаётся черновиком до утверждения |
| Возвраты учитываются | инвариант «выручка = `/crm/plans`» ослаблен до «с точностью до возвратов периода», подсказка на обоих экранах |
| Новый партнёр — по доступной истории | кэш новизны с флагом неполноты; партнёр без отгрузок Новый при первой покупке |
| Передача партнёра — с 1-го числа следующего месяца | история закрепления, атрибуция внутри месяца не дробится |
| Знаменатель дней — рабочие везде | `/crm/plans` приводится к тому же (`mot-41`) |

## Волна 1 — расчёт (экранов ещё нет)

| Карточка | Содержание | Зависимости |
|---|---|---|
| `mot-20` | Каркас: миграции `motivation_parameter_orders`, `focus_rules` + `focus_snapshot_items`, `partner_novelty`, `partner_assignments`, `debt_exclusions`, `pool_packages` + `_items`, `plan_orders`, `quarterly_bonuses` + `_qualifications`; модели, комментарии к таблицам и колонкам, исключение префикса `motivation_` из `bi:sync-grants` | — |
| `mot-21` | `VariablePartComponent` + компоненты П1/П2/П3/К1 и `TransitionGuaranteeComponent`; схема v2 в `payroll_schemes`; персональный слой ставок; контрольный тест из восьми примеров раздела 02 | mot-20 |
| `mot-22` | Расширение сбора входов: отгрузки базы и Новых раздельно, Фокус-перечень по снимку, база К1 из регистра с учётом исключений, возвраты | mot-20, mot-21 |
| `mot-23` | Кэш новизны партнёров + бэкфилл реестра закрепления; команда пересчёта, флаг неполной истории | mot-20 |
| `mot-24` | Квартал: премия, поклиентная квалификация, распределение по работникам; команда расчёта квартала | mot-20 |
| `mot-25` | План: расчёт базы из медианы по рабочим дням, уменьшение при отсутствии, ограничитель снижения 20 %, вето на повышение; приказ о планах | mot-20 |

## Волна 2 — рабочее место менеджера

| Карточка | Содержание | Зависимости |
|---|---|---|
| `mot-26` | «Мой месяц»: доход, разложение с объяснениями, план и порог, рычаги, прогноз, калькулятор; пункт меню, опрос | mot-21…23 |
| `mot-27` | «Моя база», «Кто выпал из ритма» — поверх `OpportunityService::signals()` | mot-22 |
| `mot-28` | «Долги» с группировкой по партнёрам, разделом исключённых, пометкой неразобранных | mot-22 |
| `mot-29` | «Кого разбудить», «Мои новые клиенты», «Свободные клиенты» | mot-23 |
| `mot-30` | «Фокус-товары», «Откуда мой план», «Премия отдела за квартал» | mot-24, mot-25 |
| `mot-31` | Расчётный лист: экран, PDF, версии, возражение работника | mot-21 |

## Волна 3 — рабочее место руководителя

| Карточка | Содержание | Зависимости |
|---|---|---|
| `mot-32` | «Параметры мотивации»: шесть групп, приказы с датой действия, история с диффом, предпросмотр эффекта, валидация связностей, вкладка персональных отклонений | mot-21 |
| `mot-33` | Мастер планов на квартал: расчёт, сравнение с действующим планом, вето, ограничитель снижения, утверждение приказа | mot-25 |
| `mot-34` | Сводка отдела, ведомость к утверждению, корректировки, возражения | mot-21 |
| `mot-35` | Пул и раздача: пакеты, сроки, возвраты в Пул, состояние крана | mot-23 |
| `mot-36` | Фокус-перечень: правила, снимок состава, блок отдачи | mot-22 |
| `mot-37` | Исключения задолженности, очередь разметки накладных с ценой в рублях | mot-22 |
| `mot-38` | Здоровье базы, квартальная премия и её распределение, прогноз фонда, журнал скидок | mot-24 |

## Волна 4 — переход

| Карточка | Содержание | Зависимости |
|---|---|---|
| `mot-39` | Параллельный расчёт: два снимка на переходный период, блок сравнения у менеджера, колонки у руководителя | mot-21, mot-34 |
| `mot-40` | Гарантия переходного периода: фиксация базы среднего, доплата отдельной строкой | mot-21 |
| `mot-41` | Приведение «Планов продаж» к рабочим дням; выключение множителя активных клиентов в схеме v2 | mot-25 |
| `mot-42` | Производственный календарь 2027; строка о домене в `CLAUDE.md`; обновление документации | — |

## Объёмы, под которые проектируем

Считано на боевой базе 08.09.2026; полностью — `docs/sales/tz/prilozhenie-profil-dannyh.md`.

| Список | Строк сегодня | Решение |
|---|---|---|
| «Моя база» | 113 и 103, из них 58 и 50 никогда не покупали | пагинация 50, отдельная вкладка непокупавших |
| «Долги» | 198 и 70 накладных у 16 и 14 партнёров | двухуровневый список: партнёр, накладные внутри |
| «Кто выпал из ритма» | 9 и 17 при фильтрах, до 80 без них | пагинация включается при снятых фильтрах |
| «Кого разбудить» | 58 и 50 | пагинация 50, две вкладки |
| Пул | 618, покупали когда-либо 4 | пагинация, фильтр по умолчанию даёт 4 строки |
| Очередь разметки | 196 и 44 за квартал | сортировка по влиянию на расчёт в рублях |
| Трекер премии | 12 кандидатов, засчитано 4 | проектировать и под почти пустое состояние |
| Мост накладных | 2 423 и 832 за три месяца | журнал только с пагинацией |

## Предусловия запуска (не задачи разработки)

| Условие | Ответственный | Без него |
|---|---|---|
| Табель заполняется ежемесячно | руководитель отдела | план не уменьшается при отсутствии, медиана считается по неотработанным дням, надбавка за замещение не начисляется |
| Производственный календарь 2027 | разработка, до декабря 2026 | январские праздники станут рабочими, план Q1 завышен примерно на треть |
| Фокус-перечень заполнен приказом | руководитель отдела | П3 не начисляется |
| Реестр закрепления ведётся | руководитель отдела | история не накапливается, атрибуция идёт по текущему состоянию |
| Старая задолженность расчищена | руководитель, финансовая служба | 53 накладные у 11 партнёров на 1 524 тыс ₽; без расчистки система стартует с вычета за долги, которыми работник не занимается |
| Правка Приложения № 1 | заказчик | «расчёт фиксируется 5-го числа» → «представляется к утверждению 5-го числа» |

## Критерии готовности эпика

- [ ] `PayrollCalculatorGoldenTest` зелёный; схема v1 и её умолчания не изменены
- [ ] Восемь контрольных примеров раздела 02 сходятся до копейки, включая пример 1 из Приложения № 3 (120 400 ₽) и сквозной на живых данных (Сухов, август, 161 685 ₽)
- [ ] Все инварианты раздела 09 покрыты автотестами
- [ ] Каждая строка Приложения № 1 имеет поле на «Параметрах мотивации»; параметров, правящихся кодом или миграцией, не осталось
- [ ] Персональное отклонение параметра применяется только к своему работнику и только в своём периоде
- [ ] Утверждённый расчёт не меняется при изменении параметров, отгрузок, платежей и закрепления
- [ ] Выручка в расчёте совпадает с `/crm/plans` с точностью до возвратов периода; расхождение объяснено подсказкой на обоих экранах
- [ ] Квартальная премия не входит в месячный доход ни в одном представлении
- [ ] Все таблицы `motivation_*` имеют комментарии и исключены из `bi:sync-grants`
- [ ] Параллельный расчёт показывает менеджеру разложение разницы, а не только итог
- [ ] Гарантия переходного периода выводится отдельной строкой расчётного листа
- [ ] `make verify` зелёный перед выкаткой на прод
