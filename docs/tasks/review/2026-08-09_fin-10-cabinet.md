# fin-10 · Деньги возвращаются в кабинет клиента

**Приоритет:** высокий
**Создано:** 2026-08-09
**Эпик:** [fin-00](2026-08-09_fin-00-epic.md)
**Зависимости:** `fin-07`, `fin-06` зелёный на **проде**
**Волна:** 3 — цель всего эпика

## Описание

2026-08-09 деньги в личном кабинете скрыли целиком: `CABINET_FINANCE_ENABLED=false`,
обоснование — «показывать клиенту завышенный долг хуже, чем не показывать никакого».
Эта карточка возвращает их обратно, но уже с цифрами, которые сходятся с 1С.

Клиент хочет знать ровно две вещи (формулировка заказчика):

1. **Сколько я должен прямо сейчас.**
2. **Дошёл ли мой платёж.**

Всё остальное — по запросу, не на первом экране.

## Что делаем

### Снятие флага

`config/cabinet.php` → `finance_enabled` в `true`, `.env.example`, dev, затем прод.
Middleware `EnsureCabinetFinanceEnabled` и сам флаг **не удалять** до `fin-11`: нужен
быстрый откат, если на боевых данных что-то всплывёт.

Точки, где флаг гейтит данные (все уже размечены spread-паттерном):
`CabinetController:28-38`, `ShipmentController:51,86-95,182-220,443-475`,
`OrderController:511-520`, `routes/user.php:235-247`, `CabinetLayout.jsx:30,100-107`.

### Дашборд

Блок баланса на главной кабинета — одно крупное число «К оплате сейчас» и дата ближайшего
платежа. Разрез по нашим организациям с банковскими реквизитами уже реализован
(`CabinetController::balanceByOrganization()`), источник меняется на регистр.

Переплата показывается как переплата, а не как «долг −5000 ₽».

### Раздел «Оплаты»

`resources/js/Pages/User/Cabinet/Payments/{Index,Calendar,Show}.jsx` — журнал, календарь,
карточка. Источник журнала остаётся `payments` (документ), календарь и остаток — регистр.

> ✅ **Оба бага расчёта уже исправлены (2026-08-11)**, не дожидаясь снятия флага.
>
> 1. `scheduleQuery()` не выбирал `prepaid_amount`, а `scheduleEntry()`/`sumUnpaid()`
>    считали `unpaid = amount − paid_amount`. Клиент увидел бы просрочку, которой нет:
>    1С разносит на заказы почти половину денег.
> 2. Рядом нашёлся второй: в просрочку попадали **все** строки с прошедшей датой,
>    включая полностью оплаченные. Сумма выходила нулевой, а счётчик показывал
>    «Просрочено: 5 документов».
>
> Оба сейчас скрыты флагом, но чинить их вместе со снятием флага значило бы менять
> цифры и включать раздел одним движением — при расхождении не понять, что виновато.
> Покрыто `PaymentsCabinetTest`: `order_prepayment_closes_calendar_line`
> и `order_prepayment_is_not_counted_as_overdue`.

### Новое: акт сверки для клиента

Тот же экран, что в [`fin-08`](2026-08-09_fin-08-crm.md), но в кабинете и только по своим
контрагентам и юрлицам. Клиент сам видит, из чего сложился долг — это снимает часть звонков
менеджеру.

### Карточки документов

Реализация (`ShipmentController:443-475`) — блок оплаты и график из регистра.
Заказ (`OrderController:511-520`) — предоплата.

## Edge-кейсы

- **Клиент видит долг, которого не признаёт** → акт сверки на том же экране, с реквизитами
  документов. Это и есть ответ.
- **Переплата** → «У вас переплата N ₽», не отрицательный долг.
- **Долг перед несколькими нашими юрлицами** → разрез с реквизитами каждого; переплата одному
  **не гасит** долг другому — взаимозачёт делает 1С (уже зафиксировано в `rules/organizations.md`).
- **Платёж отправлен, но не дошёл** → в журнале его нет. Показать пояснение «платежи появляются
  после проведения в учёте, обычно в течение дня», иначе клиент решит, что деньги пропали.
- **Регистр пуст для клиента** → «нет данных за период», а не «долг 0 ₽».
- **Период до 01.01.2026** → пояснение про дату начала регистра.
- **Сотрудник смотрит кабинет** → `is_staff` управляет ценами витрины, к деньгам отношения
  не имеет. Не путать.

## Что сделано (2026-08-12)

Код написан целиком, **флаги не трогали** — раздел кабинета остаётся закрытым.
Включение будет отдельным решением после зелёной сверки на проде.

| Сделано | Как |
|---|---|
| Дашборд | Наверху «К оплате сейчас» и дата ближайшего платежа вместо сальдо |
| Переплата | Отдельной строкой, а не отрицательным долгом |
| Разрез по юрлицам | Из регистра, реквизиты по-прежнему из справочника |
| Календарь оплат | План из регистра, форма ответа не изменилась |
| Акт сверки клиента | Новый экран `/cabinet/payments/reconciliation` |

**Главное число экрана поменялось со смысла.** Раньше показывали сальдо, но в него
входят обязательства, срок которых ещё не наступил, — клиент читал их как «должен
прямо сейчас». Теперь наверху стоит наступивший долг, а сальдо ушло справочным.

**Знак у суммы не показывается.** «−55 000 ₽» клиент читает как ошибку сайта;
показываем модуль и подпись, которая объясняет, в чью пользу.

**Два флага, а не один.** `cabinet.finance_enabled` закрывает раздел целиком,
`settlements.ledger_enabled` выбирает источник. Регистр можно включить для CRM,
пока кабинет ещё закрыт, — и это проверено тестом.

**Баннер расхождения для клиента сформулирован иначе, чем для менеджера.** Менеджеру
пишем «акт неполный, отправлять нельзя», клиенту — «данные уточняются, сверьтесь
с менеджером». Внутреннюю кухню про регистр и учётную систему клиенту знать незачем,
ему важно, что цифру рано считать окончательной.

Тесты: `tests/Feature/User/CabinetLedgerFinanceTest.php` — 8 кейсов, включая изоляцию
чужих движений и поведение при каждом из флагов.

## Критерии готовности

- [ ] `settlements:verify` на **проде** зелёный — **ждёт данных 1С**, это гейт включения, не разработки
- [ ] `CABINET_FINANCE_ENABLED=true` — **не включали намеренно**
- [x] Дашборд показывает «К оплате сейчас» и дату ближайшего платежа
- [x] Переплата отображается как переплата
- [x] Журнал оплат, календарь, карточка платежа работают
- [x] Баг `scheduleQuery()` с `prepaid_amount` закрыт; заодно закрыт счётчик просрочки
- [x] Акт сверки доступен клиенту по своим контрагентам и нашим юрлицам
- [ ] Цифра в кабинете **совпадает** с CRM и с 1С до копейки — сверить на трёх живых клиентах
- [x] Весь интерфейс на русском
- [x] `PaymentsCabinetTest` зелёный в обоих режимах флага
- [x] Флаг и middleware сохранены для отката
- [ ] Прод: после релиза дождаться 200 на pecado.ru — упавший деплой оставляет maintenance
