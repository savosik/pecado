# ЭПИК: Совместная отгрузка резервов — одна реализация и один расходный ордер (ship-00)

**Приоритет:** высокий
**Создано:** 2026-09-18
**Статус (22.09.2026): 🚀 раскатано на всех участников резервов.** Протокол v16.11.0 согласован (топик №7)
и испытан на боевом контуре (топик №8, 21.09, все 10 случаев Р-7 зелёные с обеих сторон). Правки после прогона
стоят в бою у обеих сторон (1С: D8, D9; сайт: S1–S3, `50f731ba`). 22.09 по команде заказчика снята канарейка:
`ORDER_SHIP_TOGETHER_ENABLED=true` без `ORDER_SHIP_TOGETHER_CANARY`, режим доступен 89/89 участникам. Откат —
`ORDER_SHIP_TOGETHER_ENABLED=false` + `config:cache` + `queue:restart`. Регрессии — тестовый партнёр
«ТЕСТ Совместная отгрузка» (`a39e877c-…`) по чек-листу Р-7 через новый топик Agent Hub.

## Идея

Интернет-магазины держат по нескольку резервов и отправляют их в отгрузку сразу, одним курьером.
Клиент просит один документ реализации на выбранные резервы, склад — один расходный ордер.
Слить заказы средствами сайта нельзя (правка резерва только на уменьшение, «новый заказ + отмена
резервов» даёт гонку за остаток). Решение: **объединяется отгрузка, а не заказы** — заказы остаются
отдельными документами, 1С оформляет по группе минимальный комплект реализаций и ордеров.

## Контракт (аддитивно)

- `order.confirmed` → 1С: `ship_together_key` (UUID группы) + `ship_together_order_uuids` (манифест,
  минимум 2, одинаков во всех сообщениях группы). Только вместе.
- `order.updated` ← 1С: `ship_together_key`, `ship_together_status` (`confirmed` | `conflict`),
  `ship_together_conflict {reason, message, order_uuid}`; коды `timeout`, `manifest_mismatch`,
  `not_reserved`, `incompatible`, `merge_forbidden`, `shortage`, `processing_error`.
- Правила: `docs-erp/content/rules/order-reserves.md#совместная-отгрузка-резервов-v16110`,
  тест-план `docs-erp/content/tests/reserves.md` (Р-7), changelog 16.11.0.

## Сделано на сайте (ветка)

- Проверки группы до публикации (`ShipTogetherService`): один партнёр, контрагент, валюта, **ровно один склад**
  (`OrderWarehouseResolver` — тот же, что даёт `warehouse_uuids` в `order.created`), способ и адрес доставки,
  все заказы в резерве и не в ожидании. Сообщения группы — одной транзакцией, одинаковый ключ и манифест.
- Резерв локально **не снимается**: `orders.ship_together_status = pending`, правки/отмена/повторная отправка
  закрыты (409 `ship_together_pending`). Итог применяет `HandleOrderUpdated`: успех → резерв снят, отказ →
  заказ снова в резерве с причиной; `reserve:false` без полей группы (старая 1С) снимает ожидание как одиночное.
- Письмо `orders.ship_together_rejected` — одно на группу (origin_key по ключу группы), включено умолчанием
  как остальные резервные поводы.
- Страховка: `reserve:ship-together-timeout` каждые 5 минут — группа без итога дольше
  `ORDER_SHIP_TOGETHER_PENDING_TIMEOUT_MINUTES` (60) возвращается в резерв с локальной причиной `no_response`.
- Кабинет `/cabinet/reserves`: галочки на карточках, «В отгрузку вместе (N)», бейдж «ждём склад», причина отказа;
  карточка заказа — плашка ожидания вместо кнопок. Клиентский API v1 / MCP: операция `reserves.confirm_group`,
  поле `ship_together` в `reserves.list` и карточке заказа.
- Тесты: `tests/Feature/Order/ShipTogetherConfirmTest.php`, `tests/Feature/Erp/ShipTogetherOutcomeTest.php`,
  `tests/Feature/Api/Client/ClientApiShipTogetherTest.php`.

## Испытания (топик №8, 21.09.2026)

- 1С: часть установлена в `trade` целиком (с 20.09). Два блокера у них: **D1** предконтроль сравнивал дату отгрузки
  буквально (резервы разных дней → `incompatible`; чинят: сравнение по фактической дате, контракт не меняется);
  **D3** регламентное задание тайм-аута ни разу не стартовало — нужен перезапуск рабочих процессов кластера.
  Рубильник не включать до их «готово, включайте».
- Сайт: канарейка `ORDER_SHIP_TOGETHER_CANARY` (UUID партнёров через запятую); команда
  `reserve:ship-together-trial send|resend|republish|status` — группа с придержанным сообщением, досылка и повтор
  теми же `message_id` (payload в `storage/app/ship-together-trials/`); отказ группы не возвращает резерв
  принудительно (`reserve` из `order.updated` авторитетен).
- План прогона: реальный клиент (кандидат в день прогона: K1 с пропуском → повтор → K2 целиком → повтор) — Р-7.1/2/3/7/8/9;
  тестовый партнёр ИП Исько (`5645311c-4885-11f1-8e85-86588116ffe9`) — Р-7.4/7.5 (правки данных с разрешения владельца 1С);
  `shortage` (Р-7.6) — только на стенде 1С. Повторный `order.updated` на поздние сообщения не нужен.
- Состояние группы на стороне 1С: регистры `Доп_ГруппыСовместнойОтгрузки`, `Доп_ЗаказыСовместнойОтгрузки`,
  ledger `Доп_СообщенияРезервовСайта` — 1С присылает снимки в payload.

## Прогон Р-7 на проде — 21.09.2026 15:00–15:50 МСК, все 10 случаев пройдены с обеих сторон

Тестовый партнёр «ТЕСТ Совместная отгрузка» (`a39e877c-…`, user 859, остаётся для регрессий), шесть резервов
29УТ-014663…014668 по 1 шт. артикулов 3012/763008. Группы: A,B `merge_forbidden` → C,D `incompatible` → E,F
`not_reserved` (с досылкой) → K1 тайм-аут (задание 1С закрыло ровно через 15 мин) → повтор K1 → K2 успех
(`confirmed` за 16 с, один ордер УТ-00012764 `prepared` + одна реализация 29УТ-008299 на оба заказа, порядок
`goods_issue.created` → `shipment.created` → `order.updated`) → повтор K2 → одиночный C. Документы 1С распровела,
заказы закрыты. Отклонения без изменения контракта: 1С **D8** (`order_uuid` не null при запрете на партнёре),
**D9** (итог отказа только дошедшим заказам) — отдельная поставка; сайт **S1** (исход «подтверждён» при снятии
резерва без полей группы), **S2** (дубль письма при позднем итоге), **S3** (старый бейдж отказа после одиночного
«В отгрузку») — исправлены, коммиты `ce446a75`, `50f731ba`. `proposal` (seq 29): режим остаётся с канарейкой на
тестового партнёра до установки D8/D9 и выкатки S1–S3, снятие канарейки — решение владельца сайта, ориентир 22.09.

## Осталось до включения

1. Сторона 1С по resolution топика №7 (накопитель, предконтроль, один групповой вызов движков, откат).
2. Совместные испытания на dev по Р-7 (успех, тайм-аут, недобор, разные склады, повтор сообщений).
3. Включение на проде: `ORDER_SHIP_TOGETHER_ENABLED=true` (+ на прогон `ORDER_SHIP_TOGETHER_CANARY=<uuid Буселя>,<uuid Исько>`) в `/srv/pecado/.env` + `config:cache` + `queue:restart` + `horizon:terminate` (воркеры держат кеш конфига). Аварийно выключить — тем же флагом обратно.
4. Испытания идут через топик Agent Hub №8 (`docs/ship-together/agent-hub/`), ход у 1С с 19.09.2026.
5. Дописать в буклет клиентам (docs/pickup) абзац про «отправить вместе»; legacy client-api (`/api/client-api/{token}`)
   групповой операции не получает намеренно.
6. Решение заказчика: «минимальный комплект» реализаций принято агентом сайта; если нужна жёсткая «ровно одна»,
   переигрывать новым топиком.
