# rop-handover: передача дел РОП Астапенко → Елисеев (разовая операция на проде)

**Приоритет:** срочно
**Создано:** 2026-08-16
**Эпик:** `abs-00` (соседний, использует те же понятия карточка/учётка)

## Что делает команда `crm:rop-handover`

По умолчанию сухой прогон с отчётом, запись только с `--apply`. Идемпотентна.

1. Учётка Павла (paxa333@gmail.com, была «Большая Красная Шляпа») → имя
   «Елисеев Павел», роль `sales-head`, `user_kind = staff`.
2. Карточка менеджера Астапенко (без erp_uuid, ~620 клиентов) → имя «Елисеев
   Павел», email `sales@pecado.ru`, привязка к учётке Павла. Клиенты, лиды,
   планы и промо-аудитории ссылаются на id карточки — переезжают сами, 1С не
   перетрёт (erp_uuid пуст).
3. Открытые `crm_tasks` и все `crm_task_recurrences` Астапенко → на Павла.
   История (комментарии/звонки/письма/закрытые задачи) не переписывается.
4. Учётка Астапенко: `status = blocked` (middleware разлогинит), снятие ролей
   (fallback задач по недоборам берёт первого sales-head!), отзыв
   `crm_agent_tokens`, удаление `entity_subscriptions`.

## Чек-лист боевого запуска

- [ ] **Создать ящик sales@pecado.ru** (закрывает todo `create-sales-mailbox`) —
      без него письма о заказах 620 клиентов уйдут в никуда.
- [ ] Задеплоить ветку, на проде: `php artisan crm:rop-handover` (сухой прогон,
      сверить счётчики с ожиданиями: карточка ~620 клиентов).
- [ ] `php artisan crm:rop-handover --apply`.
- [ ] Проверить: вход Астапенко отлуплен; кабинет любого клиента Астапенко
      показывает «Елисеев Павел» / sales@pecado.ru; Павел входит в /crm.
- [ ] Переименовать колонку менеджера в Google-таблице «План 2026»
      (`SalesSheetImporter` матчит по имени карточки).
- [ ] Переименовать партнёра «Большая Красная Шляпа» в 1С — иначе
      `partner.updated` вернёт старый `erp_name`.
- [ ] Завести отсутствие: Курочкина, отпуск, по 2026-08-31, замещает Сухов
      (через /crm/absences под Павлом или tinker).

## Заметки

- Павел остаётся привязан к своей же карточке как клиент (сам себе менеджер) —
  безвредно, письма о его собственных заказах придут на sales@pecado.ru.
- Роль делает учётку staff — витрина цен переключится в staff-режим.
