# fin-07 · Прогноз поступлений и презентер на регистре

**Приоритет:** высокий
**Создано:** 2026-08-09
**Эпик:** [fin-00](2026-08-09_fin-00-epic.md)
**Зависимости:** `fin-06` зелёный
**Волна:** 3 — за флагом `SETTLEMENTS_LEDGER_ENABLED`

## Описание

Счётное ядро переезжает на `settlement_entries`. Ключевое — сервис **сильно упрощается**:
исчезает половина причин, по которым он был сложным.

| Что уходит | Почему |
|---|---|
| `noScheduleQuery()` (~50 строк) | Долг без графика — обычная строка регистра, а не отдельная ветка |
| `amount - paid_amount - prepaid_amount` | Одна колонка `settled_amount` от 1С |
| Сведение валют по текущему курсу | `amount_rub` посчитан на дату операции при приёме |
| Джойн `shipment_payment_schedules × shipments × users × organizations` | Разрезы лежат в самой строке регистра |

Что **сохраняется** как есть — оно рабочее: `FinanceFilters`, `AGING_BUCKETS`,
`applyDefaultOrder()`, структура `row()`, XLSX-экспорт, изоляция через `User::visibleInCrm()`.

## Что делаем

### `app/Services/Crm/Finance/PaymentForecastService.php`

Переписать запросы на `settlement_entries`. Соответствие методов:

| Метод | Было | Стало |
|---|---|---|
| `plannedQuery()` | `shipment_payment_schedules` + 4 джойна | `nature='plan'`, `unsettled > EPSILON` |
| `noScheduleQuery()` | Отдельная ветка | **Удалить** |
| `factsByDay()` | `payments.date`, `CASE direction` | `nature='fact' AND type IN (payment_in, payment_out)` — знак уже в `amount` |
| `aging()` | По `due_date` графика | По `date` плановых строк |
| `balances()` | `ContractorBalance` | `SUM(amount) WHERE nature='fact'`, ось контрагент × организация |
| `summary()` | 10+ подсчётов из трёх источников | Те же KPI, один источник |
| `topDebtors()` | Как есть | Источник другой |
| `toRub()` | Текущий курс из `Currency` | `amount_rub` из строки |

Новое в `summary()`: **фактический баланс** (акт сверки) рядом с текущей задолженностью —
раньше эти два числа не различались, что и было источником путаницы.

⚠️ Сохранить приём из текущего кода: в `sumByDay()`/`sumRub()` использовать `select()`,
а не `selectRaw()` — иначе `only_full_group_by` уронит запрос на MySQL. SQLite это молча
проглатывает, тестами не ловится.

### `app/Support/Payments/PaymentSchedulePresenter.php`

Переписать на плановые строки регистра.

> ✅ **Дефект агрегата уже исправлен (2026-08-11), не дожидаясь волны 3.** Шапка считала
> `unpaid = total − paid` без `prepaid_amount`, тогда как построчный `unpaid_amount` его
> вычитает: при авансе по заказу итог блока не сходился с суммой строк под ним. Это видно
> клиентам CRM и админки сегодня, флагом кабинета не закрыто, и ждать переключения чтения
> было незачем. Теперь итог собирается **суммированием строк**, а не разностью:
> переплата по одной строке не гасит долг по другой. Покрыто
> `tests/Unit/Support/PaymentSchedulePresenterTest.php` (6 кейсов).

### Флаг

`config/settlements.php` → `SETTLEMENTS_LEDGER_ENABLED` (по умолчанию `false`).
Ветвление — в одной точке, а не по всему сервису: фабрика, отдающая старую или новую
реализацию за общим интерфейсом. Иначе `if` расползётся по двадцати методам и волна 4
станет невыполнимой.

## Edge-кейсы

- **Флаг выключен** → поведение байт в байт прежнее. Тестом.
- **Регистр пуст, флаг включён** → отчёт пустой, а не «нулевой долг у всех». Показывать
  явное предупреждение, а не молчать.
- **Движение без договора** → попадает в отчёт в группу «Без договора», не теряется.
- **Валюта без курса на дату** → `amount_rub = NULL`, строка видна в детализации с исходной
  суммой, но не участвует в рублёвых итогах. Счётчик таких строк — в сводке.
- **Клиент с переплатой** → положительный баланс, в просрочку и в «кому звонить» не попадает.

## Критерии готовности

- [x] Расчёт на регистре реализован — `LedgerPaymentForecastService`; ветки «долг без
      графика» в нём нет
- [x] Флаг выключен → все существующие тесты зелёные без изменений
- [ ] Флаг включён → те же числа, что даёт `settlements:verify` — **ждёт данных из 1С**
- [x] `PaymentSchedulePresenter` — агрегат сходится с суммой строк
- [x] В `summary()` появился `balance_fact` отдельно от текущей задолженности
- [x] Агрегаты с `groupBy` проверены тестом формы SQL (как в `FinanceScopeTest`)
- [x] Ветвление по флагу в одной точке — привязка в `AppServiceProvider`
- [x] `FinanceScopeTest` зелёный; ledger покрыт `FinanceLedgerTest`
- [x] XLSX-экспорт не тронут: он строится над тем же контрактом

## Что сделано (2026-08-11)

**Интерфейс вместо класса.** `PaymentForecast` — контракт, две реализации,
выбор привязкой в контейнере по `settlements.ledger_enabled`. Контроллеров-потребителей
всего два, так что подмена типа обошлась двумя строками.

**Контроллеры перестали знать схему.** Они дописывали к возвращённому запросу
`whereDate('sch.due_date', ...)` — псевдоним таблицы и имя колонки протекали наружу,
а у регистра плановая дата лежит в `date`. Условия по сроку вынесены в интерфейс
методами `overdueOnly()` и `dueBetween()`.

**Общая часть — в трейт `FormatsForecastRows`.** Форматирование строки, корзины
времени, сведение валют: ~250 строк, одинаковых для обеих реализаций. Дублировать
их значило бы получить расхождение в подписях при переключении флага —
и стало бы невозможно отличить «поехали цифры» от «поехало оформление».

**Область ledger-реализации — паритет, а не расширение.** Плановые строки берутся
только по документам вида `shipment`: сегодняшний интерфейс сплошь «реализация»,
строка ведёт на карточку реализации. Показать там заказ значило бы дать неработающую
ссылку. Это ровно тот состав, что даёт старая реализация, — переключение флага
не меняет набор строк. Предоплаты по заказам приходят вместе с интерфейсом в `fin-08`.

**Найдена ловушка границ периода.** Каст `date` пишет в SQLite «2026-08-11 00:00:00»,
и `whereBetween` по голой колонке отсекал строку, попадающую ровно на верхнюю границу.
Молча и только на краю диапазона. Везде, где сравнивается плановая дата, теперь `DATE()`.

Тесты: `tests/Feature/Crm/FinanceLedgerTest.php` — 13 кейсов, включая проверку
формы агрегатов и то, что по умолчанию отдаётся старое ядро.
