# ЭПИК: Самовывоз 9–21 — статусы сборки, пропуск курьеру, выдача на складе (pick-00)

**Приоритет:** высокий
**Создано:** 2026-09-17
**Статус (18.09.2026):** код эпика **на проде** (коммит `15fe04de`, релиз через `dev` → `main`, полный набор
тестов зелёный, 4865). Рубильники `PICKUP_ENABLED` и `PICKUP_WMS_ENABLED` выключены — включение по решению
заказчика прямо на проде, без стенда (его решение 18.09). Перед включением обязательно `PICKUP_HANDOVER_SINCE`:
без него команда напоминаний отказывает, а очередь склада заполнится историей. Приёмка по `docs/pickup/plan-testirovaniya.pdf`
на проде. Не сделано: настоящие снимки новых экранов в буклете, живой скан расходного листа, тексты сайта.

## Идея

Склад переходит на график **пн–сб, 9:00–21:00**. Интернет-магазины (96 % заказов) получают
розничные заказы вечером и в выходные, и теперь могут закрывать их без нас: поставили заказ
в резерв → подтвердили у своего покупателя → нажали «В отгрузку» (или отменили) → отправили
курьера на склад. Менеджеры работают пн–пт 9–18 и в этом цикле не участвуют. Это наш ответ
на доставку маркетплейсов «за 4 часа»: магазин остаётся конкурентным, потому что товар
у него на руках через час после звонка покупателю.

Резервы уже в бою (res-00, 84 участника). Не хватает второй половины цикла:

1. **Клиент слеп после кнопки «В отгрузку».** Он не видит, собран ли заказ, ждёт ли выдачи,
   забрал ли его курьер, за какими заказами можно посылать.
2. **Склад слеп на выдаче.** Нет списка «к самовыдаче», нет способа проверить курьера, нет
   отметки «выдан» — в 1С такого статуса нет и не будет.
3. **Курьеров может быть несколько.** Один забирает часть заказов, остальное ждёт другого.
4. **Нет материалов.** Без буклета и инструкций новый график станет снегом на голову для
   клиентов, склада и отдела продаж.

Процесс выдачи строим по образцу ПВЗ маркетплейса: у клиента в кабинете **пропуск** (QR и
шестизначный код), кладовщик сканирует его телефоном, видит, что отдать, и отмечает выдачу.

## Решения (заказчик, 17.09.2026)

1. **График склада:** пн–сб 9:00–21:00. **SLA сборки 30–40 минут.**
2. **«Час до закрытия» = приём к сборке до 20:00.** Заказ, отправленный в отгрузку до 20:00,
   собирается и выдаётся сегодня до 21:00; позже — сборка со следующего рабочего дня склада
   с 9:00. Клиент видит это обещание в момент отправки. Смысл правила — курьер не звонит
   кладовщику с просьбой задержаться.
3. **Верификация курьера — код выдачи из кабинета** (QR + 6 цифр). Клиент пересылает курьеру
   пропуск только на те заказы, которые надо забрать.
4. **Скан расходного листа не обязателен.** Пропуск отвечает на вопрос «что и кому отдать»;
   скан листа — необязательная проверка «та ли коробка». Запуск не зависит от того, как 1С
   формирует штрихкод.
5. **Менеджеров вне их рабочего дня не дёргаем** (кроме форс-мажора). Единственный стоп
   на пути заказа к складу — задолженность (`DebtGate`, уже работает).
6. **Порядок работ:** эпик и карточки → три PDF → код по волнам.

**Умолчания, принятые без отдельного вопроса:**

- Единица выдачи — **расходный ордер (РО)**: это физические коробки. Клиент мыслит заказами,
  поэтому в кабинете заказы сгруппированы по РО, чекбокс стоит на группе. РО нельзя поделить
  между двумя курьерами — об этом прямо сказано в интерфейсе и в буклете.
- Пропуск двух видов: **«на всё готовое»** (умолчание, состав считается в момент скана) и
  **«на выбранное»**. Срок жизни — до конца третьего рабочего дня склада, клиент может отозвать.
- Всё за рубильником `PICKUP_ENABLED` (`config/pickup.php`), включение — по команде заказчика.
- История до запуска в список «Ждут выдачи» не попадает (`pickup.handover_since`).

## Решения (заказчик, 18.09.2026)

1. **Письмо «собран, ждёт выдачи» включено умолчанием** — подтверждено.
2. **Недобор: менеджер замену не подбирает.** Клиенту уходит письмо «не хватило, закажите другое»
   (`orders.items_unavailable`, включено умолчанием). В окне резерва отмена строк недобором не считается — это правки клиента.
3. **По QR курьер ничего не подписывает** — пропуск и есть верификация. Без пропуска склад убеждается, что курьер
   от клиента, любым способом, и курьер расписывается в расходном листе.
4. **Собранное храним сколько угодно.** Клиенту напоминаем письмом на 3, 7, 14 и 30-й день (`orders.pickup_waiting`,
   команда `pickup:remind-waiting`). Срок пропуска (три рабочих дня) — про пропуск, а не про хранение.
5. **Сводка «пока не работали» уходит и руководителю отдела продаж** — по всему отделу, включая клиентов без менеджера.
6. **1С создаёт расходные ордера автоматически** — главный блокер снят. Наблюдение про 24 заказа без ордера на копии
   базы — снимок момента, а не дыра процесса.
7. **Объединение резервов в одну реализацию — отдельным эпиком, не сейчас.** Требование к pick-00: ничего не сломать.
   Выполнено: единица выдачи — расходный ордер на любое число заказов; письмо «собран» одно на комплект, а не на заказ;
   пропуск и экран склада работают с комплектом целиком. Постановка топика для 1С лежит в `docs/pickup/agent-hub/`, не отправлена.
8. **Штрихкод расходного листа — алгоритм подтверждён владельцем 1С** (код функции получен): GUID ссылки без дефисов
   в нижнем регистре → десятичное число без разделителей (`Формат(…, "ЧГ=0")`), Code128. Совпадает с `DocumentBarcode`
   один в один. Не известно только, ссылка какого документа берётся (ордер, реализация, заказ) — резолвер пробует все три.

## Факты из живых данных (1500 РО с 08.08.2026, проверено 17.09.2026)

| Факт | Следствие |
|---|---|
| Все 290 самовывозных РО проходят `to_ship → shipped` за 0–5 минут, **до приезда курьера** | «Собран, ждёт выдачи» = `shipped` без записи выдачи. `prepared…to_ship` = «собирается» |
| `shipped` обратим: откаты в `to_pick` (38), `prepared` (26), отмена (18) | Стадию считаем на лету, не храним. Откат уже выданного РО → пометка `needs_review` |
| `goods_issues.delivery_type` пуст во всех строках | Самовывоз берём с заказа: `orders.delivery_method = pickup` через `goods_issue_items.order_uuid` |
| `goods_issues.user_id` пуст у 40 %, `goods_issue_items.order_id` пуст у 24 % строк | Клиента определяем от заказа и только по `order_uuid` (как `GoodsIssue::relatedOrders()`) |
| 228 РО покрывают несколько заказов (до 30); у 46 заказов по два РО; 12 РО смешанные; у 132 РО заказов нет | Стадия заказа = минимум по его РО; смешанный РО считаем самовывозом с пометкой; РО без заказа — только ручной поиск |
| `checking` и `checked` в данных не встречаются | На экранах не выделяем |
| У 199 клиентов несколько юрлиц | Пропуск принадлежит пользователю, юрлицо — группировка на экране |

**Штрихкод печатной формы типовой 1С** (подсистема «Штрихкодирование печатных форм», УТ 11 /
КА 2): Code128, число = GUID документа без дефисов, переведённый из шестнадцатеричной системы
в десятичную; контрольного символа сверх штатного Code128 нет. Обратно: десятичное → hex →
дополнить нулями слева до 32 знаков → разбить 8-4-4-4-12. GUID расходных ордеров, реализаций
и заказов у сайта есть. Гипотеза подтверждается на пяти реальных листах (pick-08).

## Архитектура

| Что | Где (файлы) | Меняется |
|---|---|---|
| Рубильник и настройки | `config/pickup.php` (`enabled`, `handover_since`, `pass_ttl_working_days`, `cancel_window_hours`, `stale_days`) | новый |
| Расписание склада | `config/warehouse.php`, `app/Services/Warehouse/WarehouseSchedule.php` | новый; праздники из `config/production_calendar.php` |
| Стадия исполнения | `app/Enums/OrderFulfilmentStage.php`, `app/Services/Pickup/OrderFulfilmentResolver.php` | новый; пакетный расчёт без N+1 |
| Выдача | таблицы `pickup_handovers`, `pickup_scan_misses`; `app/Models/Pickup/*`, `app/Services/Pickup/HandoverService.php` | новый; документ сайта вне шины, как `DeliveryShipment` |
| Пропуск | таблицы `pickup_passes`, `pickup_pass_items`; `app/Services/Pickup/PickupPassService.php` | новый |
| Кабинет клиента | `app/Http/Controllers/User/PickupController.php`, `resources/js/Pages/User/Cabinet/Pickup/Index.jsx`; правки `User/OrderController`, `Cabinet/Orders/{Index,Show}.jsx`, `Cabinet/Dashboard.jsx`, `CabinetLayout.jsx`, `utils/orderTimeline.js`, `HandleInertiaRequests` | раздел «Самовывоз», бейджи стадий |
| Страница курьера | `GET /p/{token}`, `resources/js/Pages/Pickup/Pass.jsx` | новый; без входа |
| Экран склада | `app/Http/Controllers/Wms/PickupController.php`, `resources/js/Wms/Pages/Pickups/*`, `routes/wms.php`, `resources/js/Wms/config/menuConfig.ts` | новый; mobile-first |
| Права | `wms-pickups.view / issue / cancel` — `RolesAndPermissionsSeeder`, `Admin\RoleController`, меню | три реестра, страж `PermissionNamingTest` |
| Штрихкод 1С | `app/Support/Erp/DocumentBarcode.php`, `app/Services/Pickup/ScanResolver.php` | новый |
| Уведомления | `config/mail_occasions.php`, `MailTagBuilder`, слушатель смены статуса РО | `orders.ready_for_pickup`, `orders.handed_over` |
| API и MCP | `app/Services/Client/Api/Operations/PickupOperations.php`, правка `OrderOperations`, `FeatureGate::PICKUP` | блок `fulfilment`, операции `pickup.*` |
| CRM и админка | карточка заказа, карточка партнёра | стадия и факт выдачи |
| Материалы | `docs/pickup/_src/*.src.html` → `docs/pickup/*.pdf` | буклет и две инструкции |

Переиспользуем: `ClientOrderActions`, `ReservePolicy`, `ReserveCountdown.jsx`,
`BarcodeCameraView.jsx` (ZXing читает и Code128, и QR), режимы ввода из
`Pages/User/Cart/BarcodeScanner.jsx`, удержание фокуса под USB-сканер из
`Wms/Pages/Defects/Quick.jsx`, `chillerlan/php-qrcode`, `DocumentNumber::normalize()`,
`shared/Panel/{RowActions,ConfirmDialog,useConfirmDelete}`.

## Стадии заказа глазами клиента

| Стадия | Когда | Подпись |
|---|---|---|
| `reserved` | `orders.reserve = true` | В резерве |
| `sent_to_warehouse` | резерв снят, РО ещё нет | Передан на склад (+ обещание «соберём к ~14:40» или «начнём в пн в 9:00») |
| `picking` | есть РО в `prepared…to_ship` | Собирается |
| `ready` | самовывоз, все РО `shipped`, выдачи нет | Собран, ждёт выдачи |
| `handed_over` | есть запись выдачи по всем РО | Выдан курьеру (кем, когда) |
| `shipped` | доставка, все РО `shipped` | Отгружен |

Промежуточные состояния («собран частично», «выдан частично») получаются сами: стадия заказа —
минимальная по его РО, а счётчик «2 из 3 мест» показывается рядом.

## Нарезка на карточки

### Волна 0 — постановка и материалы

| Карточка | Содержание | Зависимости |
|---|---|---|
| `pick-00` | Эпик, рубильник `config/pickup.php` | — |
| `pick-01` | Буклет клиентам и две инструкции в PDF | черновик сразу; скриншоты новых экранов — после pick-05, pick-07, pick-09 |

### Волна 1 — минимум для запуска графика

| Карточка | Содержание | Зависимости |
|---|---|---|
| `pick-02` | Расписание склада `WarehouseSchedule` | — |
| `pick-03` | Стадия исполнения заказа и резолвер | — |
| `pick-04` | Обещание времени сборки клиенту | pick-02 |
| `pick-05` | Стадии в кабинете: список, карточка, таймлайн, дашборд | pick-03 |
| `pick-06` | Таблицы выдачи, `HandoverService`, права | — |
| `pick-07` | Экран склада `/wms/pickups`: список и ручная выдача | pick-03, pick-06 |
| `pick-08` | Штрихкод расходного листа 1С: декодер и скан | pick-07 |

### Волна 2 — пропуск курьеру

| Карточка | Содержание | Зависимости |
|---|---|---|
| `pick-09` | Пропуска и раздел кабинета «Самовывоз» | pick-03 |
| `pick-10` | Публичная страница курьера с QR | pick-09 |
| `pick-11` | Скан пропуска и кода на складе, «Выдать всё» | pick-08, pick-09 |

### Волна 3 — уведомления, API, хвосты графика

| Карточка | Содержание | Зависимости |
|---|---|---|
| `pick-12` | Уведомления клиенту «собран» и «выдан» | pick-03, pick-06 |
| `pick-13` | Утренняя сводка менеджеру; сводка недоборов под новый график | pick-02 |
| `pick-14` | Клиентский API и MCP | pick-03, pick-09 |
| `pick-15` | Стадия и факт выдачи в CRM и админке | pick-03, pick-06 |
| `pick-16` | Часы работы на сайте, разбор расхождений, PWA склада | pick-07 |

### Волна 4 — эксплуатация (после включения на проде)

| Карточка | Содержание | Зависимости |
|---|---|---|
| `pick-17` | Вход кладовщика по ссылке без пароля, киоск, журнал выдач по ссылке | pick-07 |
| `pick-18` | Стойка выдачи: технические перерывы (двое подменяют друг друга), «Отойти»/«Вернулся», статус «выдают сейчас» и телефон склада у курьера и клиента; камера умолчанием на телефоне, сканер — на компьютере | pick-07, pick-09, pick-10 |

## Критерии готовности эпика

- [ ] Клиент в кабинете и через API видит по каждому заказу: собирается, собран и ждёт выдачи, выдан.
- [ ] При отправке в отгрузку клиент видит честное обещание времени с учётом графика и отсечки 20:00.
- [ ] Кладовщик на телефоне видит список к выдаче, сканирует пропуск, выдаёт часть или всё, видит просрочку SLA.
- [ ] Двойная выдача невозможна, ошибочная отменяется с причиной, всё с автором и временем.
- [ ] Клиент может отдать разным курьерам разные заказы.
- [ ] Менеджер утром видит, что произошло вечером и в субботу, без звонков.
- [ ] Буклет и две инструкции в PDF разосланы до включения рубильника.
- [ ] История до запуска не всплыла в списке «Ждут выдачи».
- [ ] Контракт ERP не тронут; `PermissionNamingTest`, `db:comments:audit --strict`, `make verify` зелёные.

## Чек-лист включения

1. ~~Расходный ордер создаётся без менеджера~~ — подтверждено заказчиком 18.09.2026: 1С создаёт ордера сама.
2. Ветка слита в `dev`, зелёный CI, визуальная приёмка экранов на `dev.pecado.ru` с телефона.
3. Один реальный расходный лист отсканирован на экране выдачи — убедиться, что находится нужный ордер (алгоритм
   подтверждён, неизвестно только, чья ссылка в штрихкоде).
4. Макеты в PDF заменены снимками, PDF пересобраны (`bash docs/pickup/build.sh`), тексты сверены с ответами на вопросы.
5. Прод: `PICKUP_HANDOVER_SINCE=<дата и время запуска>`, затем `PICKUP_WMS_ENABLED=true` — склад учится отмечать выдачу,
   клиенты ещё ничего не видят. Права `wms-pickups.*` приходят сидером при деплое.
6. Инструкции выданы складу и отделу продаж, буклет разослан клиентам с датой старта.
7. `PICKUP_ENABLED=true`, в тот же день через админку обновить страницы «Контакты», «Доставка» и ответ FAQ про самовывоз.
8. `bi:sync-grants` — решить, открывать ли таблицы `pickup_*` BI-агенту.
9. Первая неделя: смотреть `pickup_scan_misses`, вкладки «Зависшие» и «Разбор», утреннюю сводку менеджерам.

Аварийно погасить всё: `PICKUP_ENABLED=false` и `PICKUP_WMS_ENABLED=false`. Данные о выдачах сохраняются.

## Наблюдение с копии боевой базы (17.09.2026) — снято решением № 6 от 18.09

Из 50 последних заказов 24 находятся в стадии «Передан на склад»: статус «Готов к отгрузке», а расходного ордера нет.
Это либо снимок момента, либо подтверждение открытого вопроса № 1 — ордер появляется не сам. Проверить до запуска:
если ордер создаёт менеджер, вечерний заказ простоит до утра, а клиент будет видеть «Передан на склад» с обещанием,
которое склад не выполнит.

## Открытые вопросы перед стартом

**Закрыты решениями 18.09.2026: № 1, 2, 4, 5, 6. Остаётся № 3 (попросить 1С заполнять `delivery_type`) — не срочно.**

**Исходный список:**

1. **Кто создаёт расходный ордер вечером и в субботу.** В описании статусов: «подготовлен —
   менеджер создал РО». Если РО не создаётся автоматически, заказ после 18:00 до склада не
   дойдёт, и весь график не заработает. Вопрос владельцу 1С — до рассылки буклета.
2. **Недобор при сборке вне рабочего дня менеджера.** Сейчас `orders.shortfall` адресован
   менеджеру. Кто и как сообщает клиенту вечером, что позиции нет?
3. **Попросить 1С заполнять `delivery_type` в РО.** Поле в контракте уже есть, контракт не
   меняется; снимет зависимость от связи РО с заказом.
4. **Включать ли `orders.ready_for_pickup` умолчанием.** Предлагаем да, как резервы: иначе
   клиент узнает о готовности, только открыв кабинет.
5. **Что курьер подписывает при получении** (УПД, доверенность, печать) — формулировка для буклета.
6. **Пять реальных расходных листов** (фото) — подтвердить формат штрихкода.

**Закрываем сами при реализации:**

- Один график на все склады; адрес выдачи — из конфига (у 528 РО `warehouse_id` пуст).
- Смешанный РО (самовывоз и доставка) показываем в выдаче с пометкой.
- Откат `shipped` после уведомления «собран»: повторное письмо только после нового `shipped`.
