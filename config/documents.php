<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Раздел «Документы» в личном кабинете
    |--------------------------------------------------------------------------
    |
    | Флаг закрывает ТОЛЬКО показ клиенту: маршруты кабинета отдают 404, пункт
    | меню и ссылки в карточках заказа и реализации не рендерятся.
    |
    | Приём печатных форм из 1С работает ВСЕГДА, независимо от флага. Иначе
    | документы, присланные раньше открытия раздела, потерялись бы безвозвратно:
    | 1С печатные формы не хранит, и перезалить их неоткуда. Тот же принцип,
    | что у erp.organizations.enabled.
    |
    */
    'enabled' => (bool) env('DOCUMENTS_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | Витрина документов в CRM
    |--------------------------------------------------------------------------
    |
    | Отдельным флагом от клиентского раздела: менеджеры должны сверить типы,
    | номера и содержимое PDF раньше, чем документы увидит клиент.
    |
    */
    'crm_enabled' => (bool) env('DOCUMENTS_CRM_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Хранилища
    |--------------------------------------------------------------------------
    |
    | `exchange_disk` — обменный бакет: 1С кладёт PDF, сайт забирает и удаляет
    | исходник. Живёт по своей ретенции, доступ на запись есть у 1С.
    |
    | `disk` — долговременное приватное хранилище сайта. У 1С доступа туда нет,
    | публичных ссылок диск не выдаёт: файл отдаётся только через контроллер
    | с проверкой прав.
    |
    | Диски разведены намеренно. Обменный бакет обязан чиститься, документы нужны
    | годами, и один бакет с двумя ретенциями означал бы уборщик, который однажды
    | снесёт живой счёт-фактуру.
    |
    */
    'disk' => env('DOCUMENTS_DISK', 'printed-documents'),
    'exchange_disk' => env('DOCUMENTS_EXCHANGE_DISK', 'documents-exchange'),

    /*
    |--------------------------------------------------------------------------
    | Ретенции
    |--------------------------------------------------------------------------
    |
    | `exchange_retention_days` — через сколько дней documents:clean-exchange
    | сносит из обменного бакета файлы, которые сайт не забрал. Страховка от
    | «зомби»: сообщение потерялось, а объект остался.
    |
    | `trashed_retention_days` — через сколько дней после отзыва формы
    | documents:prune удаляет её файл. Не сразу: снятие пометки удаления в 1С —
    | обычная операция, а перезалить PDF заново неоткуда.
    |
    */
    'exchange_retention_days' => (int) env('DOCUMENTS_EXCHANGE_RETENTION_DAYS', 7),
    'trashed_retention_days' => (int) env('DOCUMENTS_TRASHED_RETENTION_DAYS', 90),

    /*
    |--------------------------------------------------------------------------
    | Требования к файлу
    |--------------------------------------------------------------------------
    |
    | Принимается только PDF. Раздел обещает клиенту документ, который открывается
    | в браузере одним кликом; ZIP-архивы и офисные форматы этого обещания не
    | выполняют, а разбирать их сайт не будет.
    |
    */
    'max_file_size' => (int) env('DOCUMENTS_MAX_FILE_SIZE', 52428800), // 50 МБ

    /*
    |--------------------------------------------------------------------------
    | Уведомления о новых документах
    |--------------------------------------------------------------------------
    |
    | Механизм подписок (config/subscriptions.php) готов, секция «documents»
    | заведена выключенной. Включение — отдельной задачей, после того как раздел
    | откроется клиентам и станет понятен реальный поток документов.
    |
    */
    'subscriptions_enabled' => (bool) env('DOCUMENTS_SUBSCRIPTIONS_ENABLED', false),

];
