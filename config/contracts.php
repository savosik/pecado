<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Раздел «Договоры» в личном кабинете партнёра
    |--------------------------------------------------------------------------
    |
    | Флаг закрывает ТОЛЬКО показ клиенту: маршруты кабинета отдают 404, пункт
    | меню не рендерится. Реестр в CRM работает независимо от флага — менеджеры
    | должны выверить реестр (после импорта таблицы) раньше, чем его увидит
    | партнёр. Дополнительно у каждого договора есть свой флажок
    | `is_visible_in_cabinet`.
    |
    */
    'cabinet_enabled' => (bool) env('CONTRACTS_CABINET_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Предупреждение об окончании срока
    |--------------------------------------------------------------------------
    |
    | За сколько дней до `valid_until` договор считается «истекающим» и
    | подсвечивается в реестре.
    |
    */
    'expiring_days' => (int) env('CONTRACTS_EXPIRING_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | Диск для сканов договоров
    |--------------------------------------------------------------------------
    |
    | Приватный: договор — не картинка товара, публичной ссылки у него быть
    | не должно. Партнёр и менеджер получают файл только через контроллер,
    | который проверяет принадлежность. В медиатеку админки сканы не попадают.
    |
    */
    'attachments_disk' => env('CONTRACTS_ATTACHMENTS_DISK', 'crm-attachments'),

];
