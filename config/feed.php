<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Публичный YML-фид Яндекс.Маркета
    |--------------------------------------------------------------------------
    |
    | warehouse — название склада, по наличию на котором формируется фид:
    | в выгрузку попадают только товары с остатком > 0 на этом складе,
    | а <count>/available берутся из его остатка. Значение сверяется с
    | warehouses.name; менять можно через env YANDEX_FEED_WAREHOUSE без кода.
    |
    */

    'yandex_market' => [
        'warehouse' => env('YANDEX_FEED_WAREHOUSE', 'Москва Основной'),
    ],

];
