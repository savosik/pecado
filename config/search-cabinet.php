<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Fuzzy для документов кабинета
    |--------------------------------------------------------------------------
    |
    | Подключает Meilisearch fuzzy-поиск по составу заказов / возвратов /
    | реализаций (через Searchable trait на OrderItem/ReturnItem/ShipmentItem).
    | При выключенном флаге используется только LIKE-логика контроллеров.
    | См. docs/cabinet-search-roadmap.md → Волна 4 → PR 4.2.
    |
    */
    'fuzzy_documents' => env('CABINET_SEARCH_FUZZY_DOCUMENTS', false),

    /*
    |--------------------------------------------------------------------------
    | Fuzzy для товарных разделов
    |--------------------------------------------------------------------------
    |
    | Подключает fuzzy в searchProducts / Favorites. См. PR 4.3 (не реализован).
    |
    */
    'fuzzy_products' => env('CABINET_SEARCH_FUZZY_PRODUCTS', false),

    /*
    |--------------------------------------------------------------------------
    | Лимит документов из Meilisearch на один запрос
    |--------------------------------------------------------------------------
    |
    | Сколько строк-позиций возвращать из Scout перед редукцией к ID документов.
    | Дальше идёт DB-фильтрация по user_id и pluck по document_id.
    |
    */
    'fuzzy_documents_limit' => (int) env('CABINET_SEARCH_FUZZY_DOCUMENTS_LIMIT', 500),

    /*
    |--------------------------------------------------------------------------
    | Лимит товаров из Meilisearch на один запрос
    |--------------------------------------------------------------------------
    |
    | Сколько товаров возвращать из Scout-источника для поиска в favorites /
    | searchProducts. Дальше идёт DB-фильтрация (favorites.user_id, фасеты).
    |
    */
    'fuzzy_products_limit' => (int) env('CABINET_SEARCH_FUZZY_PRODUCTS_LIMIT', 500),

    /*
    |--------------------------------------------------------------------------
    | Сохранённые поиски (пресеты)
    |--------------------------------------------------------------------------
    |
    | Включает CRUD для пользовательских пресетов фильтров (`UserSearchPreset`)
    | и компонент `<SavedSearches />` на страницах кабинета. См. PR 5.1.
    | При выключенном флаге API-маршруты возвращают 404, UI скрыт.
    |
    */
    'presets' => env('CABINET_SEARCH_PRESETS', false),

    /*
    |--------------------------------------------------------------------------
    | Экспорт результатов поиска (CSV/XLSX)
    |--------------------------------------------------------------------------
    |
    | Включает эндпойнты `/cabinet/{section}/export?format=csv|xlsx` для
    | Orders/Returns/Shipments и кнопку «Экспорт» на страницах. См. PR 5.2.
    | При выключенном флаге API возвращает 404, UI скрыт.
    |
    */
    'export' => env('CABINET_SEARCH_EXPORT', false),

    /*
    |--------------------------------------------------------------------------
    | Подсказки для пустой выдачи (EmptyState)
    |--------------------------------------------------------------------------
    |
    | Когда поиск/фильтры дают пустую выдачу, серверный хелпер
    | `EmptyResultSuggestion` формирует человеческий совет «что можно
    | сделать» и передаётся фронту в `suggestion`. См. PR 5.3.
    |
    */
    'suggestions' => env('CABINET_SEARCH_SUGGESTIONS', false),

];
