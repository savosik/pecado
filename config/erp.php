<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Логирование сообщений шины ERP
    |--------------------------------------------------------------------------
    |
    | Когда включено, все входящие и исходящие сообщения RabbitMQ
    | сохраняются в таблицу erp_bus_messages для отладки.
    | В production по умолчанию выключено.
    |
    */
    'bus_logging_enabled' => env('ERP_BUS_LOGGING_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | Ретенция лога шины и журнала дедупликации
    |--------------------------------------------------------------------------
    |
    | `erp_bus_messages` — самая тяжёлая таблица боевой базы после сноса Pulse
    | (454 MB на месяц логов). Держим 14 дней в БД, всё старше уезжает в
    | холодное хранилище командой `erp:cleanup-messages` (см. bus_archive).
    |
    | `erp_processed_messages` — журнал дедупликации входящих: нужен только на
    | горизонт повторной доставки RabbitMQ, до августа 2026 не чистился вовсе.
    |
    | 0 в любом из них — «не чистить», как у notifications.mail.journal_retention_days.
    |
    */
    'bus_retention_days' => env('ERP_BUS_RETENTION_DAYS', 14),
    'processed_retention_days' => env('ERP_PROCESSED_RETENTION_DAYS', 14),

    /*
    |--------------------------------------------------------------------------
    | Архив лога шины в холодном хранилище
    |--------------------------------------------------------------------------
    |
    | Пока выключено, `erp:cleanup-messages` просто удаляет старые записи —
    | как и раньше. Когда включено, сутки сначала выгружаются в S3 (Yandex
    | Object Storage, ледяной класс) и удаляются только после успешной заливки.
    |
    | Включать имеет смысл лишь там, где прописаны креды ERP_ARCHIVE_* —
    | иначе команда будет честно падать, ничего не удаляя.
    |
    */
    'bus_archive' => [
        'enabled' => env('ERP_BUS_ARCHIVE_ENABLED', false),
        'disk' => env('ERP_BUS_ARCHIVE_DISK', 'erp-archive'),
        'prefix' => env('ERP_BUS_ARCHIVE_PREFIX', 'erp-bus'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Публикация contractor.created в 1С (US-07 v13.2)
    |--------------------------------------------------------------------------
    |
    | При создании Company с непустым tax_id (или первом заполнении tax_id
    | на апдейте) сайт публикует contractor.created в очередь erp_out.contractors.
    | 1С генерирует свой UUID и возвращает его через contractor.updated.
    | Флаг позволяет быстро отключить publisher без деплоя, если что-то пошло не так.
    |
    */
    'publish_contractors' => env('PUBLISH_CONTRACTORS_TO_ERP', true),

    /*
    |--------------------------------------------------------------------------
    | Shovel с московского ESB (внешние остатки)
    |--------------------------------------------------------------------------
    |
    | RabbitMQ Shovel тянет сообщения об остатках из очереди на ESB
    | (`remains_for_moscow`) и публикует их в локальный fanout-обменник
    | `external.remains`, откуда они расходятся по двум очередям:
    |   - external.remains_for_website (потребитель — сайт Pecado)
    |   - external.remains_for_erp     (потребитель — 1С)
    |
    | Параметры shovel-а задаются на нашем rabbitmq через Management HTTP API
    | в команде `php artisan rabbitmq:setup`. Если `src_uri` пустой —
    | shovel не создаётся (локальный dev без доступа к ESB).
    |
    */
    'moscow_shovel' => [
        'name' => 'moscow-remains',
        'src_uri' => env('MOSCOW_ESB_AMQP_URI'),
        'src_queue' => env('MOSCOW_ESB_SRC_QUEUE', 'remains_for_moscow'),
        'dest_exchange' => 'external.remains',
        'prefetch_count' => (int) env('MOSCOW_ESB_SHOVEL_PREFETCH', 1000),
        'reconnect_delay' => (int) env('MOSCOW_ESB_SHOVEL_RECONNECT_DELAY', 5),
    ],

    /*
    |--------------------------------------------------------------------------
    | Shovel с ESB Andrey Company (заказы из чужой 1С)
    |--------------------------------------------------------------------------
    |
    | RabbitMQ Shovel тянет заказы из очереди `pecado.orders` на ESB Andrey
    | (esb.services.andrey.company:45671, plain AMQP) и публикует их в локальный
    | fanout-обменник `external.orders_from_andrey` → очередь
    | `external.orders_from_andrey_for_erp` (потребитель — 1С Pecado).
    |
    | Сценарий: «прокидывание» заказов из 1С Andrey в 1С Pecado через шину.
    | Сайт сам эти сообщения не обрабатывает. Fanout оставлен (а не прямой
    | shovel-в-очередь), чтобы при необходимости легко добавить второго
    | потребителя (например, очередь для отладки) одним bind-ом.
    |
    | Если `ANDREY_ESB_AMQP_URI` пустой — shovel не создаётся (локальный dev / CI).
    |
    */
    'andrey_shovel' => [
        'name' => 'andrey-orders',
        'src_uri' => env('ANDREY_ESB_AMQP_URI'),
        'src_queue' => env('ANDREY_ESB_SRC_QUEUE', 'pecado.orders'),
        'dest_exchange' => 'external.orders_from_andrey',
        'prefetch_count' => (int) env('ANDREY_ESB_SHOVEL_PREFETCH', 100),
        'reconnect_delay' => (int) env('ANDREY_ESB_SHOVEL_RECONNECT_DELAY', 5),
    ],

    /*
    |--------------------------------------------------------------------------
    | TTL сообщений в очередях external.remains_for_*
    |--------------------------------------------------------------------------
    |
    | Применяется через RabbitMQ policy (pattern `^external\.remains_for_.*$`).
    | По умолчанию 3 дня — совпадает с TTL очереди-источника на ESB,
    | чтобы в локальных очередях не накапливалось больше, чем на источнике.
    |
    */
    'external_remains_ttl_ms' => (int) env('EXTERNAL_REMAINS_TTL_MS', 3 * 24 * 60 * 60 * 1000),

    /*
    |--------------------------------------------------------------------------
    | Consumer внешних остатков (external.remains_for_website)
    |--------------------------------------------------------------------------
    |
    | Потребитель очереди external.remains_for_website принимает события
    | `product.quantity.updated` из московского ESB (полная карточка товара
    | с массивом остатков по 12 складам). В БД сайта мы используем остатки
    | только по одному складу — «Тюмень Основной», UUID которого задаётся
    | ниже. Остальные склады в сообщении игнорируются.
    |
    | Доступный остаток вычисляется как max(0, quantity - reserve).
    |
    */
    'external_remains' => [
        'tyumen_warehouse_uuid' => env(
            'EXTERNAL_REMAINS_TYUMEN_WAREHOUSE_UUID',
            'f8083799-0838-11e0-a1ea-505054503030',
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | Защита ручных привязок к ProductModel
    |--------------------------------------------------------------------------
    |
    | На сайте часть моделей товаров и их привязок к товарам ведётся вручную
    | через artisan-команду `products:apply-model-bindings` из JSON-артефакта.
    | Такие ProductModel создаются БЕЗ external_id (UUID 1С) — это маркер
    | «ручной» модели.
    |
    | При включённом флаге обработчики ERP product.created/product.updated:
    |   1) не перезаписывают Product.model_id, если у товара уже есть привязка;
    |   2) не проставляют external_id в существующую ProductModel, найденную
    |      по name с пустым external_id (это наша ручная модель — оставляем
    |      её «ручной»).
    |
    | Выключите (false), если хотите вернуть приоритет 1С как мастер-каталога
    | для моделей.
    |
    */
    'product_models' => [
        'preserve_existing' => env('ERP_PRESERVE_PRODUCT_MODEL', true),

        /*
         | Полная заглушка приёма моделей из 1С (временный тумблер).
         |
         | При true обработчики product.created/product.updated ВООБЩЕ не
         | принимают данные о модели из payload:
         |   - существующим товарам Product.model_id не трогается;
         |   - новым товарам model_id остаётся null (карточка-одиночка).
         | Используется, пока привязки к ProductModel ведём вручную на сайте
         | и не хотим, чтобы 1С пересобирала связки по generic-«модели».
         | Сильнее, чем preserve_existing (тот защищает лишь непустой model_id).
         */
        'ignore_incoming' => env('ERP_IGNORE_INCOMING_PRODUCT_MODEL', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | ⚠️ ВРЕМЕННЫЙ КОСТЫЛЬ (HACK): подмена UUID склада в предзаказах
    |--------------------------------------------------------------------------
    |
    | TODO(костыль): по требованию 1С при публикации заказов типа `preorder`
    | в массиве `warehouse_uuids` исходящего сообщения UUID склада
    | «Тюмень Основной» временно подменяется на фиксированный `target_uuid`.
    |
    | Подмена работает ТОЛЬКО для предзаказов (`type = preorder`).
    | Обычные заказы (`type = order`) не затрагиваются.
    |
    | Как откатить:
    |   - быстро, без деплоя:  PREORDER_WAREHOUSE_UUID_OVERRIDE_ENABLED=false
    |   - полностью: удалить этот блок и вызов applyPreorderWarehouseOverride()
    |     в App\Listeners\PublishOrderToErp::resolveWarehouseUuids().
    |
    | source_uuid — исходный external_id склада «Тюмень Основной» в warehouses
    |               (совпадает с external_remains.tyumen_warehouse_uuid).
    | target_uuid — временный UUID, который пишем вместо него.
    |
    */
    'preorder_warehouse_uuid_override' => [
        'enabled' => env('PREORDER_WAREHOUSE_UUID_OVERRIDE_ENABLED', true),
        'source_uuid' => env(
            'PREORDER_WAREHOUSE_UUID_OVERRIDE_SOURCE',
            'f8083799-0838-11e0-a1ea-505054503030',
        ),
        'target_uuid' => env(
            'PREORDER_WAREHOUSE_UUID_OVERRIDE_TARGET',
            '3d0a3eb9-0c23-11ee-8ddc-ee348b24c7ce',
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | Стопка складов: фиксация склада в исходящем заказе (v16.5.0)
    |--------------------------------------------------------------------------
    |
    | Переходный флаг совместимости с легаси-приёмником 1С — тот же паттерн,
    | что у replaces_order_uuid (v16.2.0).
    |
    | Выключен (по умолчанию): даже для регионов со стопкой исходящий
    | order.created уходит в ПРЕЖНЕМ формате — warehouse_uuids перечисляет
    | все primary-склады региона, конкретный выбирает 1С. Стопка при этом
    | полностью работает на сайте (остатки, цены, разбиение заказов,
    | assigned_warehouse_id сохраняется в БД) — легаси-1С ничего не замечает.
    |
    | Включён: для заказа с зафиксированным складом (type=order, регион со
    | стопкой) warehouse_uuids содержит ровно один UUID — склад-победитель.
    | Включать ТОЛЬКО после подтверждения стороной 1С (вопросы 1–3 в
    | docs/erp-1c-warehouse-stack-questions.md).
    |
    | Откат без деплоя: ERP_STACK_WAREHOUSE_PINNING_ENABLED=false.
    |
    */
    'stack_warehouse_pinning' => [
        'enabled' => env('ERP_STACK_WAREHOUSE_PINNING_ENABLED', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Отправка organization_uuid в исходящем order.created (v16.5.0)
    |--------------------------------------------------------------------------
    |
    | Переходный флаг совместимости с легаси-приёмником 1С — независим от
    | stack_warehouse_pinning: 1С может подтвердить одно раньше другого.
    |
    | Выключен (по умолчанию): поле organization_uuid в payload вообще
    | не появляется — легаси-приёмник не видит новый ключ. Привязка
    | warehouses.organization_id при этом работает на сайте уже сейчас
    | (справочно, в админке).
    |
    | Включён: для заказов, где организация однозначно определена (склад
    | заказа привязан к организации на сайте), добавляется опциональное
    | поле organization_uuid. Если организация не определена — поле
    | отправляется как null (означает «1С выбирает как раньше», как и для
    | остальных опциональных полей документа).
    |
    | Включать ТОЛЬКО после подтверждения стороной 1С.
    | Откат без деплоя: ERP_ORGANIZATION_UUID_ENABLED=false.
    |
    */
    'organization_uuid_publishing' => [
        'enabled' => env('ERP_ORGANIZATION_UUID_ENABLED', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Организации (наши юрлица) — эпик org-00
    |--------------------------------------------------------------------------
    |
    | Флаг гейтит ТОЛЬКО показ графы «Продавец» в документах: админка, ЛК, CRM,
    | отчёты и выгрузки (волны 1–2 эпика).
    |
    | Сам справочник в админке флагом НЕ закрыт — он доступен по праву
    | organizations.view: юрлица с их UUID нужно завести до того, как 1С начнёт
    | присылать организацию, иначе всё приедет заглушками.
    |
    | Приём и сохранение организации из сообщений 1С работают ВСЕГДА, независимо
    | от флага. Иначе данные, присланные 1С раньше срока, потерялись бы
    | безвозвратно — а восстановить их можно только повторной выгрузкой из 1С.
    |
    */
    'organizations' => [
        'enabled' => env('ORGANIZATIONS_ENABLED', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Контрагенты, исключённые из обмена взаиморасчётами
    |--------------------------------------------------------------------------
    |
    | Маркетплейсы: 1С сознательно вырезает их из канала settlement.* и из
    | balance.updated (список «КонтрагентыИсключения» на её стороне), поэтому
    | их балансы на сайте заморожены навсегда и показывать их нельзя —
    | клиент видел бы «долг» без расшифровки и без шанса на обновление.
    | Согласовано с 1С в топике «Регистр взаиморасчётов — круг 11» (18.08.2026).
    |
    | Скрытие применяется глобальным scope-ом в ContractorBalance; админский
    | реестр балансов показывает их без scope-а — для отладки обмена.
    |
    */
    'excluded_contractor_tax_ids' => [
        '9714053621', // РВБ ООО (Wildberries)
        '9704254424', // ЯНДЕКС МАРКЕТ ООО
        '9701048328', // МАРКЕТПЛЕЙС ООО
        '9718101499', // ЯНДЕКС.ЛАВКА ООО
    ],
];
