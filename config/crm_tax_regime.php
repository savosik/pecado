<?php

/*
|--------------------------------------------------------------------------
| Налоговый режим юрлиц партнёров (оценка рисков перехода клиентов на НДС)
|--------------------------------------------------------------------------
|
| Менеджер отмечает у каждого юрлица режим «сейчас» и план на следующий год.
| Ответ не вечен: он устаревает по сроку давности и безусловно 1 января года,
| на который был план, — тогда план превратился в текущий режим, и нужен
| план на год вперёд. Устаревшие ответы по покупающим юрлицам превращаются
| в задачи менеджеру: команда crm:tax-regime-tasks по понедельникам.
|
*/

return [

    // Сколько дней ответ считается актуальным после заполнения или подтверждения.
    'confirm_days' => (int) env('CRM_TAX_REGIME_CONFIRM_DAYS', 90),

    // Если партнёр «ещё не решил» — переспрашиваем чаще: решение может появиться в любой день.
    'undecided_confirm_days' => (int) env('CRM_TAX_REGIME_UNDECIDED_DAYS', 30),

    // Юрлицо «покупающее», если по нему была реализация за столько дней.
    // Только по таким ставятся задачи и считается реестр по умолчанию.
    'active_days' => (int) env('CRM_TAX_REGIME_ACTIVE_DAYS', 365),

    'tasks' => [
        'enabled' => (bool) env('CRM_TAX_REGIME_TASKS_ENABLED', true),
        // Срок задачи в рабочих днях от постановки.
        'due_weekdays' => (int) env('CRM_TAX_REGIME_TASK_DUE_WEEKDAYS', 5),
        // Тег задачи: по нему команда находит свои задачи, не плодит дубли и закрывает выполненные.
        'tag' => 'Налоговый режим',
    ],

    // Опрос клиента на сайте: ярлычок сбоку, приглашение в кабинете и после заказа.
    // Ответ клиента записывается так же, как ответ менеджера, с пометкой источника.
    'client_survey' => [
        'enabled' => (bool) env('CRM_TAX_SURVEY_ENABLED', true),
        // «Не сейчас» прячет приглашение на столько дней.
        'snooze_days' => (int) env('CRM_TAX_SURVEY_SNOOZE_DAYS', 14),
        // После стольких «Не сейчас» приглашения не показываются, остаётся ярлычок.
        'max_snoozes' => (int) env('CRM_TAX_SURVEY_MAX_SNOOZES', 2),
    ],

];
