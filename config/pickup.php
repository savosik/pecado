<?php

/*
 * Самовывоз: стадии сборки, пропуск курьеру, выдача на складе (эпик pick-00).
 */
return [
    // Глобальный рубильник. Выключен — клиент и склад ничего нового не видят.
    'enabled' => env('PICKUP_ENABLED', false),

    // С какого момента собранные заказы попадают в «Ждут выдачи» (Y-m-d H:i или пусто).
    // Без отсечения при включении всплыла бы вся история расходных ордеров.
    'handover_since' => env('PICKUP_HANDOVER_SINCE'),

    // Срок жизни пропуска — до конца N-го рабочего дня склада.
    'pass_ttl_working_days' => (int) env('PICKUP_PASS_TTL_DAYS', 3),

    // Окно, в котором ошибочную выдачу можно отменить.
    'cancel_window_hours' => (int) env('PICKUP_CANCEL_WINDOW_HOURS', 24),

    // Собранный заказ, ждущий дольше, уходит во вкладку «Зависшие».
    'stale_days' => (int) env('PICKUP_STALE_DAYS', 3),

    // Защита кода пропуска от перебора: промахов на пользователя за окно.
    'code_attempts' => 10,
    'code_attempts_window_minutes' => 10,
];
