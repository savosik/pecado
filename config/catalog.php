<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Атрибут срока годности
    |--------------------------------------------------------------------------
    |
    | Срок годности приходит из 1С как date-time атрибут (свойство товара) и
    | хранится в product_attribute_values.datetime_value. В каталоге, карточке
    | и QuickView мы показываем его компактным бейджем «до MM/YY». Опознаём
    | атрибут по стабильному slug (id зависит от окружения). На проде это
    | «Срок годности (годен до)» → slug ниже.
    |
    */

    'shelf_life_attribute_slug' => env('CATALOG_SHELF_LIFE_ATTRIBUTE_SLUG', 'srok-godnosti-goden-do'),

];
