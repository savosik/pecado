<?php

/**
 * Предзаказ — заказ товара, которого нет на складе, у поставщика.
 *
 * Срок поставки раньше жил только в устных объяснениях менеджера («7–9 дней»):
 * ни карточка товара, ни корзина, ни чекаут его не показывали, и клиент узнавал
 * о нём после оформления — и отказывался. Здесь единственный источник цифры для
 * всех поверхностей (см. App\Support\Preorder\PreorderTerms).
 */
return [

    /*
    |--------------------------------------------------------------------------
    | Ориентировочный срок поставки, дней
    |--------------------------------------------------------------------------
    | Показывается как диапазон «7–9 дней». Равные min и max дают «7 дней».
    */
    'lead_days' => [
        'min' => (int) env('PREORDER_LEAD_DAYS_MIN', 7),
        'max' => (int) env('PREORDER_LEAD_DAYS_MAX', 9),
    ],

];
