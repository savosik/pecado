<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Чтение денег из регистра взаиморасчётов (v16.0.0)
    |--------------------------------------------------------------------------
    |
    | Выключено — CRM и кабинет считают деньги по-старому: график оплаты
    | реализаций плюс расшифровки платежей. Включено — те же экраны читают
    | ленту движений `settlement_entries`, которую присылает 1С.
    |
    | Флаг существует потому, что переключение необратимо по смыслу: как только
    | долг начинает считаться регистром, старая арифметика перестаёт совпадать,
    | и «откатить на день» уже нельзя без объяснений менеджерам. Включать только
    | после того, как `settlements:verify` покажет ноль расхождений на боевых
    | данных — на пустом регистре включение покажет всем нулевой долг.
    |
    */

    'ledger_enabled' => env('SETTLEMENTS_LEDGER_ENABLED', false),

];
