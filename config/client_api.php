<?php

/*
|--------------------------------------------------------------------------
| Клиентский API v1 и MCP-сервер клиента (эпик capi-00)
|--------------------------------------------------------------------------
*/

return [

    /*
     * Журнал вызовов агентов клиентов (`client_agent_calls`): сколько дней хранить.
     * Строки старше удаляет `model:prune` по расписанию. Год — чтобы сравнивать
     * сезон с сезоном; аргументов и ответов в журнале нет, растёт он медленно.
     */
    'usage_retention_days' => (int) env('CLIENT_AGENT_CALLS_RETENTION_DAYS', 365),

];
