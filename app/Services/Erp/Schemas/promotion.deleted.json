{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "promotion.deleted",
    "title": "promotion.deleted (1С → Сайт) — промо-группа удалена",
    "description": "Удаление промо-группы по uuid. При получении: удаляется ErpPromotion и все его привязки к товарам; у каждого освободившегося товара пересчитывается соответствующий флаг (снимается, если больше нет ни одной ErpPromotion этого type, связанной с товаром).",
    "type": "object",
    "properties": {
        "event": {
            "type": "string",
            "const": "promotion.deleted",
            "description": "Тип события. Всегда «promotion.deleted». Ограничено константой в v15.16.0: до этого поле принимало любую строку, и сообщение с чужим event проходило бы валидацию этой схемы."
        },
        "message_id": {
            "type": "string",
            "minLength": 1
        },
        "uuid": {
            "type": "string",
            "format": "uuid"
        }
    },
    "additionalProperties": true,
    "required": [
        "event",
        "uuid"
    ]
}
