{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "order.updated",
    "title": "order.updated (1С → Сайт)",
    "type": "object",
    "properties": {
        "event": {
            "type": "string"
        },
        "message_id": {
            "type": "string",
            "minLength": 1
        },
        "uuid": {
            "type": "string",
            "format": "uuid"
        },
        "number": {
            "type": ["string", "null"],
            "description": "Номер заказа в 1С. Сохраняется как erp_number на сайте для коммуникации с менеджером."
        },
        "status": {
            "type": "string",
            "enum": ["pending", "confirmed", "ready_to_ship", "closed", "deleted"]
        },
        "delivery_address": {
            "type": ["string", "null"],
            "description": "Адрес доставки (строка). Сайт создаёт или находит запись в delivery_addresses."
        },
        "items": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "product_uuid": {
                        "type": "string",
                        "format": "uuid"
                    },
                    "quantity": {
                        "type": "number",
                        "minimum": 1
                    },
                    "base_price": {
                        "type": "number",
                        "minimum": 0
                    },
                    "discount_percent": {
                        "type": "number",
                        "maximum": 100
                    },
                    "final_price": {
                        "type": "number",
                        "minimum": 0
                    }
                },
                "additionalProperties": true
            }
        }
    },
    "additionalProperties": true,
    "required": [
        "event",
        "uuid"
    ]
}
