{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "stock.updated",
    "title": "stock.updated (1С → Сайт)",
    "type": "object",
    "properties": {
        "event": {
            "type": "string",
            "description": "Тип события. Всегда «stock.updated». Ограничено константой в v15.16.0: до этого поле принимало любую строку, и сообщение с чужим event проходило бы валидацию этой схемы.",
            "const": "stock.updated"
        },
        "message_id": {
            "type": "string",
            "minLength": 1
        },
        "product_uuid": {
            "type": "string",
            "format": "uuid"
        },
        "warehouse_uuid": {
            "type": "string",
            "format": "uuid"
        },
        "quantity": {
            "type": "number",
            "minimum": 0
        }
    },
    "additionalProperties": true,
    "required": [
        "event",
        "product_uuid",
        "warehouse_uuid",
        "quantity"
    ]
}
