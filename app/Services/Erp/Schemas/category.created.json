{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "category.created",
    "title": "category.created / category.updated (1С → Сайт)",
    "type": "object",
    "properties": {
        "event": {
            "type": "string",
            "description": "Тип события: «category.created», «category.updated». Ограничено перечислением в v15.16.0. Структура у этих событий общая, поэтому схема одна на несколько значений.",
            "enum": [
                "category.created",
                "category.updated"
            ]
        },
        "message_id": {
            "type": "string",
            "minLength": 1
        },
        "uuid": {
            "type": "string",
            "format": "uuid"
        },
        "parent_uuid": {
            "type": [
                "string",
                "null"
            ]
        },
        "name": {
            "type": "string",
            "minLength": 1
        },
        "is_active": {
            "type": "boolean"
        }
    },
    "additionalProperties": true,
    "required": [
        "event",
        "uuid",
        "name"
    ]
}
